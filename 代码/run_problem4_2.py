# -*- coding: utf-8 -*-
"""问题 4-2 的**交付入口**：把交付配置的旋钮钉死，再跑 problem4_2.py。

    python 代码/run_problem4_2.py

交付值：**总费 14,573,057 元**（读法 B、附件4 实际价、γ=1、span6、`G` 无上界）。

**为什么交付格取「G 无上界」而不是缺省的 `G≤5000`** —— 两条理由，缺一不可：

  ① **与问题 2 对齐**（题面要求 4-2 沿用问题 2 的思路）：问题 2 的交付表
     `结果/result2.xlsx` 本身就有 7,313 个槽超过 5,000 kW，即问题 2 走的是
     **无上界**读法。4-2 若自己另加一个 5,000 的上界，就不是"沿用问题 2"了。
  ② **无上界那一格，4-2 的计划与问题 2 的计划逐槽数值等价**（`diag_p4_2_gmax.py`
     实测：max|差| 0.0001 kWh / 21,734,671 kWh，相对 5e-12，属 LP 退化最优解的
     顶点抖动，连表内报出精度都不到）。⇒ 4-2 交付 = 「问题 2 的计划 + 波动电价的
     账单」，与 4-3 = 「问题 3 的计划 + 波动电价的账单」**严格镜像**。
     这个结构性结论只在无上界那一格成立，是本问最该拿出来的东西。

⚠ `problem4_2.py` 的**缺省**是 `P4_GMAX=5000`（自加的保守读法），跑出来是
  **14,736,353 元 —— 不是交付值**。两读法差 163,296 元（1.11%），且方向是
  **放开更便宜**。故本文件存在的主要理由就是钉住这个旋钮。

配置清单（详见 文档/问题4_求解归档.md）：

    P4_GMAX=inf     ★ 交付旋钮：购电按题面字面读法放开上界（缺省 5000 非交付值）
    P4_READING=B    0:00 不知道当天实际电价（主模型）
    GAMMAS=1.0      规划层取 γ=1（可交付覆盖率最高的那档，也是 4-2 的主体模型）
    P4_PSCALE=1.0   不缩放价格
    P4_PRICE_SRC=att4  实际价用附件 4（波动）

**本问只有一维、没有"计划层"那一维** —— `problem4_2.py` 的 LP 里只有 `e_ω` 是逐情景的，
`d`/`cm`/`S` 是一条一阶段轨迹（见该文件变量布局）。它既不是 4-3 的"确定性"格（那格连
情景下标都没有），也不是"追索"格（那格的 c/d/S 逐情景），而是**第三种东西**。要凑出
与 4-3 对称的 2×2 必须把 4-3 的整套计划层**移植**进来 —— 那是新建模，不是同步，
故**不做**。对照表见 `代码/诊断/diag_p4_2_gmax.py`（只有 G_MAX 一维，行名如实标注）。

⚠ 哨兵（完美预见下界）自己也受 `G_MAX` 约束：无上界时 **12,782,592 元**，
  `G≤5000` 时 13,014,628 元。**两者不是同一个数，不可跨行比"高出哨兵 %"。**
  另注意本问哨兵用附件 4 实际价，与问题 2/3 的常数价哨兵**不同口径**，不可并列比较。
"""
import os
import runpy

HERE = os.path.dirname(os.path.abspath(__file__))

# ⚠ 一律用 setdefault：**调用方显式设过的环境变量必须优先**。
#   直接赋值会让 `P4_WRITE=0 python 代码/run_problem4_2.py` 仍然写盘、
#   `P4_GMAX=5000 ...` 仍然跑无上界 —— 复现命令看着对、实际跑的是另一回事，而且**静默**。
#   （这是本项目反复踩的坑，taskrun §6.5；问题 4-3 的 run_problem4.py 已同样处理。）
#   交付配置是**缺省**，不是对调用方的强制。
_DEFAULTS = dict(
    P4_GMAX="inf",           # ★ 交付旋钮：购电放开上界（缺省 "5000" 是**非交付值**）
    P4_READING="B",          # 0:00 不知道当天实际电价（主模型）
    GAMMAS="1.0",            # 规划层 γ=1
    P4_PSCALE="1.0",         # 不缩放价格
    P4_PRICE_SRC="att4",     # 实际价用附件 4（波动）
    P4_WRITE="1",
)
for _k, _v in _DEFAULTS.items():
    os.environ.setdefault(_k, _v)

# P4_OUT 不设：problem4_2.py 缺省即写 结果/result4-2.xlsx
runpy.run_path(os.path.join(HERE, "problem4_2.py"), run_name="__main__")
