# 任务运行摘要（taskrun）

> 2026 高教社杯数模 C 题「微网与外部电网电力调控策略」
> 汇总日期：2026-09-11
> 用途：跨会话恢复上下文 / 交接，内容来自工作区记忆 + 本次会话进展

---

## 1. 已确认的建模约定（全局沿用）

- **效率方向**：储能效率 90% 按"充、放电各打 9 折"（往返 81%）：`SOC += 0.9·c·Δt`、放电吐 `d·Δt/0.9`。用户明确选此方向（非单向打折）。
- **不向电网售电**：题目只给购电价、输出表只有购电量，多余光伏弃光。
- **首尾 SOC 固定 6000 kWh**：`SOC(0)=SOC(24)=6000`；问题 2/3 为全年能量中性 `SOC_0=SOC_T=6000`（SOC 自由漂移、跨日连续）。
- **数据时间口径**：附件 1/2/4 均为 144 个 10 分钟区间，标签 `0:10…24:00` 是**右端点**标注（`0:10`=[0:00,0:10]）；附件 3 是 0/6/12/18 点发布的**逐小时**光伏预报。
- **result 模板 10 分钟错位**：附件 5 模板「计划购电量」行标签从 `0:10-0:20` 起步，比附件 1 错位 10 分钟，写入按 `result[k] = p[(k+1) % 144]` 循环左移对齐。
- 求解器：`scipy.optimize.linprog(method="highs")`。

## 2. 数据关键发现

- **附件 1 = 全年平均典型日**：附件 1 电价/负载/光伏 = 附件 4/附件 2 的全年平均（光伏因 4 位小数舍入差 0.04）。问题 1 用它做确定性求解。
- **附件 3 光伏预报近乎无偏**（总量误差约 1.2%），预报误差随时距近似减半（0:00 报正午 RMSE≈766 kW，6:00 报≈471 kW）。**逐小时预报须线性插值到 10min**——分段常数会把正午峰值砍成台阶，误差从 867 kW 升到插值的 524 kW。
- 附件 3 只有光伏预报，**负荷无预报**，仍靠"同星期几 4 周平均"统计预测。

## 3. 问题 1 — 已完成

确定性 LP，`result1.xlsx`。归档见 `文档/问题1_求解归档.md`。

## 4. 问题 2 — 已重解（2026-09-11 三次修正，代码 `代码/problem2.py`）

**⚠ 本节已于 2026-09-11 三次修正**：此前的点预测方案（16.3 M）与两阶段随机规划方案（15.72 M）
**都不可用** —— 它们把 `d̂` 当成 0:00 的一阶段承诺量，与情景平均充电写进同一个 SOC 等式，
于是「计划里承诺放出的电」在物理上不存在（实测 914,735 kWh 放电被 SOC 下限卡掉），
报出的总费低于「同一 ĝ 下储能完美预见」的下界 —— 任何策略都做不到。
另有口径错误：紧急购电写成 `e = max(0, L+ĉ−ĝ−P−d̂)`，把计划充电当成必须执行。

**现在的交付**：**γ=1 可交付储能计划**，开环、闭式、无模拟歧义。

> **⚠ 本节历经两次口径升级（均在 2026-09-12）**：
> ① 情景集从「同星期几的历史日（K=4）」改为**按低需求日分组**：15,134,192 → 14,341,723 元；
> ② 分组回看窗口 `GROUP_SPAN` 由 **14 补扫后改为 6**：14,341,723 → **13,846,553 元**。
> 两场合计 **15,134,192 → 13,846,553 元（−1,287,639，−8.51%）**。详见
> `文档/问题2_求解归档.md` §7 与 §6（§6 ⑤ 有完整窗口扫描表）。**下面这四条已按新口径更新**
> （旧值 15,187,755 元属**换分组之前**的闭式执行口径，勿再引用）。

- **结果**：计划费 13,226,936 + 紧急等 619,617 元 = **总费 13,846,553 元**；
  Σĝ **21,734,671 kWh**、因果执行紧急购电 **105,864 kWh**（旧 span14 口径为 23,015,544 / 226,629）；
  完美预见下界（哨兵）**12,406,053 元**（⚠ 含 `ĝ ≤ 5000 kW` 上界；自报的 12,229,461 配的是
  **无上界**读法，两条哨兵不可并列，见 §5.7）；真实 SOC 下界 **1200.0 kWh**（可交付）。
- **核心结论：决策变量必须划界** —— 0:00 只承诺购电量 `ĝ`；充放电 `c,d` 与紧急购电 `e`
  是**实时追索量**。把 `d̂` 写成一阶段承诺量正是旧方案的病根。
- **报童临界比 = 0.8**（`c_u/c_o = 4p/p`，注意不是 `5p/(5p+p)=5/6`）；有储能时两侧代价被削平，**τ 必须扫不能推**。
- **⚠ 不要引用点预测方案的价格**：同账本下点预测账面 15,094,675 元（便宜 93,080 元），
  但其真实 SOC 下界是 **−902,669 kWh**（52,409 槽越限）——**确凿不可交付**，便宜是因为把电池放穿了。
- **⚠ 不要声称本方案是「无预报在线最优策略」**：所有尝试过的因果可执行策略
  （日前 SP+滚动 MPC、报童不动点+滚动 MPC、γ=1 的 ĝ + 滚动 MPC）最好只有 19,443,989 元，
  比开环计划**贵 28%**。瓶颈在执行器的跨时段 SOC 管理（标量末端影子价格 λ 呈 bang-bang），
  严格修法是多阶段非预期性模型，**未构建**。详见 `文档/问题2_求解归档.md` §5。
- 情景集现为**按低需求日分组**的历史日（`problem2.py: GROUP_SPAN = 6`，回看 6 天内的同组日），
  与问题 3 的 `K_SCEN`（日内 LP 残差场景数，留出法定为 **20**）**不是同一个东西**，勿混。
  **⚠ `problem2.py` 用 span 6、`problem3_v3.py` 用 span 7 —— 两问回看窗口有意不一致**：
  各自扫描后各取最优，且实测**方向相反**（P2 偏好 6、P3 偏好 7；P3 改 6 会贵
  **32,095 元**（确定性基线，v3）/ **10,721 元**（追索基线，现行定稿））。
  完整扫描表见 §6 ⑤、P3 的实测见 §6 ⑥。**这不是待修的不一致，是两问各自的最优。**
- 归档：`文档/问题2_求解归档.md`（定稿）、`文档/问题2_独立复核_可交付性与追索模型.md`（证据）；脚本 `problem2.py`（唯一求解器）。

## 5. 问题 3 — 已完成（2026-09-12 定稿 `代码/problem3_recourse.py`，追索计划层 + 购电无上界）

**⚠ 本节历经四次修正 + 一次口径升级 + 一次结构升级**：14,722,577（口径 B，`problem3_v2.py`）
→ 14,712,825（口径 A，v3）→ 14,285,731（口径 A + 低需求日分组，v3 定稿）
→ **13,630,568（追索计划层 + 购电无上界 + τ 重调，2026-09-12 第四次定稿）**。

**用户 2026-09-11 明确指定「口径用 A」并要求另起干净求解器独立重解**；
2026-09-12 批准「采纳分组（span7）」；随后采纳「把计划层充放电改为追索变量（问题 2 两阶段）+
购电按题面字面读法放开上界」，并在本轮**批准「先重调 τ 再定稿」**。

> ★★ **本轮最重要的方法学发现：τ 必须随计划层重调。**
> 追索计划层一换，旧 τ=(0.80, 0.55) 就偏贵 **185,974 元**（13,822,501 vs 13,630,568）。
> 详见归档 §5.2。**「τ 的最优位置不随配置移动」的旧断言已作废。**

详见 `文档/问题3_求解归档.md`（§2.1 = 追索计划层，§2.3 = 基线口径，§5.2 = τ 重调，§7.2 = 四象限）。

### 5.1 计费口径：口径 A（**用户指定，以此为准**）

```
总费 = Σ p·ĝ·Δt  +  0.5·Σ p·(ĝ−g)⁺·Δt  +  1.5·Σ p·(g−ĝ)⁺·Δt  +  5·Σ p·e·Δt
       └ 计划购电费 ┘   └ 违约费 ┘          └ 超额费 ┘            └ 紧急费 ┘
```

- 题面依据：问题 2 原文「除紧急购电费用外，其他时间段的购电费用均按**计划购电量**计算」；
  问题 3 原文「总费 = 计划购电费用 + 紧急购电费用 + 调整购电量的相关费用」，而调整费只有 50% / 1.5 倍两条。
  ⇒ 计划量**全额付费**（take-or-pay）。
- **口径 B**（`p·min(g,ĝ)` 起头）**不是第三种口径**：`p·g + 0.5p·|Δ| ≡ p·min(g,ĝ) + 0.5p(ĝ−g)⁺ + 1.5p(g−ĝ)⁺`，
  与 B 代数恒等。所以 v2 就是**口径 B**。
- **两口径差 = `Σ p·(ĝ−g)⁺`**，同一轨迹下非零 ⇒ **两个口径的数不可直接比**。
  v2 报 14,722,577（B）；同轨迹换 A 结算 = **14,908,236**。A 口径下 v3 **定稿**净胜 **622,505 元**。

> **⚠ 历史记录冲突，以用户指令为准**：更早一版曾判定「计划购电费用读成 `p·ĝ`」有误并撤回。
> 用户随后明确「口径用 A」。**若再审此点，先问用户，不要自行翻案。**

**两个推论**（口径 A 特有）：

1. **最优解上违约费恒为 0。** 少报省不下 `p·ĝ`（已付清）却要付 0.5p 违约金，且真实用电还得按 1.5p/5p 补回 ⇒ 净亏。
   实测 v3 违约费 **0.00 元**（脚本内置断言）。**用途是验伪**（报出非零违约费即有 bug），**不是**用来推分位。
2. **0:00 的临界分位是分块的，不是单一值。** 0:00–6:00 块**不可调整**（只能 5p 紧急兜底）⇒ `4p/(4p+p)=0.80`；
   6/12/18 三块可 1.5p 上调兜底 ⇒ 纯理论 `0.5p/(0.5p+p)=1/3`，但**实测最优 ≈ 0.55**
   （1.5p 上调本身是在不完美预报下做的，理论值只对"确定买得到"成立）。
   **单一 τ 会抹平这个结构差别**，这就是 v2 时代 τ 曲面在 0.65–0.70 压成平台的原因。

**紧急购电在问题 3 中确实存在，价格继承问题 2 的 5 倍**（用户确认）。依据：① 问题 3 原文列举「紧急购电费用」；
② 附件 5 要求 result3.xlsx 交「**紧急购电量**」工作表；③ 问题 3 未重新定义紧急电价，按前文沿用 5 倍。

### 5.2 模型结构：四层因果

| 层 | 时刻 | 决策 | 信息 |
|---|---|---|---|
| 计划层 | 每天 0:00 | `ĝ`（144 槽）+ 计划充放电 `c, d` | 附件 3 第 0 档预报 + 历史负荷（**低需求日分组基线**，§5.3） |
| 调整层 | 6:00 / 12:00 / 18:00 | 把后续时窗目标净需求**上调** | 新发布预报 + **当天已发生的实测** |
| 执行层 | 逐槽 10 min | 跟随计划充放电、强制补放 | 实测负荷/光伏、当前 SOC |
| 结算层 | 事后 | 按口径 A 结算 | 实测轨迹 |

**只允许上调**（由推论 1，下调一分钱省不下还要付违约金）——这条不对称正是 A 与 B 的分水岭。

**三根杠杆**：

- **日前套利 LP `plan_day`**：低价充、高价放，日均充电 **18,306 kWh**（全年 6,114,270 ÷ 334 天），
  往返 `d/η ÷ c = η² = 0.8101`。题面第 7 行明确认可这一动作。**这是最大的一根杠杆。**
- **水平校正**：预报误差里"整天整体偏高/偏低"那一支**强自相关**，只取残差分位抓不到。
  必须用当天**已发生时段的【实测负荷 / 基准负荷】比值** `r` 直接测出来，再按 `r` 缩放剩余时段预报。
  `problem3_v2.py` 的 `solve_stage` 有、v3 最初漏掉，补上后紧急购电 148,649 → 59,241 kWh。
  **消融：新基线下值 132,972 元（旧基线口径下 391,377 元，见下）。**
- **★ 低需求日分组基线（2026-09-12 新增）**：`L_base` 从「前 4 个同星期几」改为
  「**过去 7 天内与第 d 天同组的日子**」的均值，组由**暖机期（前 31 天，在计费窗口之前）**
  的日均净负荷推出 = **{周五, 周六}**（周五/周六各 2 个样本，其余五天 5 个）。**无前视。**
  **收益 −427,094 元（−2.90%）**。

### 5.3 ★ 基线口径：低需求日分组（2026-09-12，本节为本次更新的核心）

**改动**：`L_base[d]` 从「前 4 个同星期几的实测负荷均值」改为「过去 7 天内同组日的均值」。

| 第 d 天 | 过去 7 天里的同组日 | `L_base[d]` |
|---|---|---|
| **周五** | 上周五、上周六 | 这 2 天的均值 |
| **周六** | 上周六、上周五 | 同上 |
| 其余五天 | 上周一~四 + 周日（5 天） | 这 5 天的均值 |

暖机期日均净负荷推出 {周五, 周六}：**Fri 42,456 / Sat 42,828** vs 其余 87,792–89,572 kWh/日
  （⚠ 2026-09-12 量纲更正：旧值 254,734 / 256,970 / 526,753–537,426 是把 144 槽功率直接求和 Σp
   却标成 kWh/日，为真实日电量 6 倍；比值 0.48 与分组结论不受影响，详见 问题2_求解归档.md §0）
（周日表现得像工作日）；**周五周六相差仅 0.9%，故可互相借用**。

**为什么回看 7 天**：这是让低需求日拿到 **2 个**同组样本的**最小整数窗口**
（span 6 只剩 1 个、span 5 一个都没有 → 回退全历史）。实测窗口严格单调：
span 7 **14,285,731** < 14 14,356,656 < 21 14,468,565 < 28 14,574,558 < 35 14,681,980。

**主对照**（334 个计费日，τ 固定为定稿值）：

| 臂 | 总费(元) | Δ vs 现状 |
|---|---:|---:|
| 旧口径 K=4 同星期几（原定稿） | 14,712,825 | — |
| 同星期几 K=1 ／ K=2 | 14,560,628 ／ 14,557,645 | −152,198 ／ −155,180 |
| **分组 span7（新定稿）** | **14,285,731** | **−427,094（−2.90%）** |
| 分组扩张（不截窗口） | 15,658,962 | +946,137 |
| 全七天一组 span14（只缩窗口） | 16,642,577 | +1,929,751 |

**★ 干净检验（照搬问题 2 的 A3−A2 结构，去掉近期性）**：分组 span7 vs 同星期几 K=1 =
**−274,896 元**，日均 −823，**95% CI [−1,062, −606]，p<0.001，中位 −469，281/334 天更省**，
剔最省 50 天后仍 −101,918；换配平基准（K=2）为 −271,914，结论不变。

> **⚠ 这里与问题 2 的关键差别**：问题 2 的同类检验（A3−A2）**CI 边缘含 0、中位日反而更贵 +1,150、
> 只 101/334 天更省、剔最省 20 天后翻正 +63,090** ⟹ 那边**不能单独主张分组这个动作显著**，
> 只能说"这个配方优于旧口径"。**问题 3 通过了同一套检验，可以主张分组本身起了作用。**

**★ 反直觉负结果：把分组搬到「残差分位取样」上是显著有害的。**
三处口径拆开独立开关（`代码/诊断/_p3_resid.py`）：

| 臂 | `L_base` | 残差分位取样 | 总费(元) | Δ vs 仅 `L_base` 分组 |
|---|---|---|---|---:|
| **R1** | **分组** | **全部历史（定稿）** | **14,356,656** | — |
| R2 | 同星期几 | 分组 | 14,727,742 | **+371,085** |
| R3 | 分组 | 分组 | 14,497,978 | **+141,321** |

配对检验 R3−R1：**p<0.001**，中位 **+263**，只 **108/334** 天更省，剔最省 50 天后差值**反而更大**（+205,236）。
**机制**：问题 3 的**残差尺度组不变**（逐槽 std 437 vs 422），分组没有东西可赚；
却把取样集从「全部历史 ~334 天」塌缩到「组内 14 天内 ~4 天」，分位估计噪声剧增 ⟹ **纯亏**。
**故 `P3_HSRC` 缺省 `all`：`L_base` 分组、残差分位池化。两处口径有意不一致，论文必须写清。**

> ⚠ **一处已更正的记录**：本文件早先记过「残差分组 p=0.257 不显著」——**那个数出自退化臂**
> （`P3_HSRC=all` 时"仅残差分组"臂根本不激活，与基线逐位相同）。显式加 `P3_HSRC=group` 后，
> 正确结论是 **p<0.001、显著有害**。**教训：负结果也要用能真正激活该机制的配置去测。**

**★ 归因：分组与水平校正是部分替代品**（两者在修同一类偏差 ⟹ **增益不可相加**）：

| 配置 | 关分组（K=4） | 开分组（span7） | 分组增益 |
|---|---:|---:|---:|
| 调整层全开（定稿） | 14,712,825 | 14,285,731 | −427,094 |
| 关水平校正（LEVEL=0） | 15,104,202 | 14,418,703 | **−685,498** |
| 关整个调整通道（ADJ=0） | 16,465,942 | 15,575,054 | **−890,888** |

**τ 不移动（⚠ 仅限确定性计划层内）**：K=4 / 分组 span14 / 分组 span7 三个配置的最优 τ
都是 **(0.80, 0.55) 内点** ⟹ 在这一层内固定 τ 比较口径是公平的，−427,094 不来自 τ 调优。
> ⚠⚠ **这条不能外推到追索计划层。** 换成追索计划后最优 τ 移到 **(0.55, 0.42)**（见 §5.5），
> 旧值偏贵 185,974 元。**"τ 的最优位置不随配置移动"的旧断言已作废。**

**⚠ `P3_GRP` 已是脚本缺省 `auto`**：复现任何"旧口径 K=4"的数**必须显式 `P3_GRP=''`**，
否则求解器跟着分组跑、对照静默退化成 `+0`（诊断脚本里踩过两次）。

### 5.4 结果（窗口 2/1–12/31，334 天，**追索 + 无上界 + 重调 τ**）

| 项 | 值 |
|---|---:|
| **总费用** | **13,630,568 元** |
| 　计划购电费 `Σp·ĝ` | 12,553,713 元 |
| 　违约费 | **0 元** ✓ |
| 　超额费 | 794,737 元 |
| 　紧急购电费 | 282,118 元 ｜ 49,632 kWh |
| `Σĝ` ／ `Σg` | 20,587,054 ／ 21,411,743 kWh |
| 储能充／放 ／ 年末 SOC | 6,629,038 ／ 5,370,112 kWh（0.8101）／ 5,760 kWh |
| 真实 SOC 日内轨迹 | **[1,200, 10,800]**，零越界 |
| 紧急购电日 ／ 无紧急日 | **210 ／ 124 天**（367 个非零槽 / 48,096） |
| 完美预见下界（无上界同口径，**交付口径**） | 12,229,461 元 ｜ 零紧急 ｜ 高出 **11.46%** |
| 完美预见下界（`G≤5000` 共同口径） | 12,406,053 元 ｜ 零紧急 |

**注**：报 SOC 区间**必须**用逐槽重建轨迹 `SOC0 + cumsum((ηc − d/η)Δt)`，
**不能用日末序列 `soc_tr`** —— 计划层把每日末端钉在 SOC0=6000，日末序列只覆盖 [5,118, 10,800]，
看着像电池没循环。

### 5.5 调参

定稿配置（交付入口 `python 代码/run_problem3.py`，四个旋钮全部钉死）：
**`P3_REC=1`、`P3_GMAX=20000`、`P3_TAUB='0.55,0.42,0.42,0.42'`、`P3_TAUP=0.42`**，
外加 `P3_BANDS=3`、`P3_LEVEL=1`、`P3_GRP=auto`、`P3_GSPAN=7`、`P3_HSRC=all`。

> ★ **脚本启动时会打印一行「本配置 = / ≠ 交付配置」的自检，以那一行为准。**
> **不要直接跑 `problem3_recourse.py` 认定交付值** —— 它缺省 `P3_GMAX=5000`（旧读法，得 14,376,005）。

**★★ τ 必须随计划层重调（本轮最重要的方法学发现）**

| τ'（固定 τ₀=0.55） | 0.36 | 0.38 | 0.40 | **0.42** | 0.44 | 0.46 | 0.48 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 总费(元) | 13,675,039 | 13,651,191 | 13,636,527 | **13,630,568** | 13,631,482 | 13,640,480 | 13,652,619 |

| τ₀（固定 τ'=0.42） | 0.50 | 0.52 | **0.55** | 0.58 | 0.60 | 0.65 | 0.70 | 0.80（旧） |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 总费(元) | 13,629,951 | 13,629,186 | **13,630,568** | 13,634,055 | 13,637,105 | 13,649,560 | 13,663,875 | 13,701,315 |

**两维都是内点最优。** τ' 左端是**真发散**（τ'=0.10 → 15,521,802 元，紧急 471,422 kWh）；
τ₀ 右端单调变贵。最优附近是宽平台：`τ₀ ∈ [0.50,0.55] × τ' ∈ [0.42,0.44]` 极差仅 **2,296 元（0.017%）**。
⇒ 论文表述为「**τ'≈0.42、τ₀≈0.55 是平台中心**」，**不得声称求得尖点最优**。

> ⚠ **旧值 (0.80, 0.55) 是确定性计划时代的定稿，原样搬到追索版偏贵 185,974 元。**
> **「τ 的最优位置不随配置移动」的旧断言已作废** —— 它只在确定性计划层内部测过。
> 机理：确定性计划层欠对冲，逼着调整层过度补偿，把 τ' 顶到 0.55；
> 追索计划层自己按情景对冲了可靠性，调整层的边际职责回落到接近纯理论的位置
> （纯理论临界比 `0.5p/(0.5p+p)=1/3`，实测 0.42，方向一致）。
> **⇒ τ 只能扫、不能推。**

**λ 在 `EXEC=plan` 下完全失效**：执行轨迹由计划层的 LP 唯一确定，λ 不进模型，
扫多个 λ 只得同一串数。λ 只在 `P3_EXEC=mpc` 下起作用。

**消融（定稿配置下重跑，τ 固定为定稿值）**：

| 配置 | 总费(元) | 相对定稿 |
|---|---:|---:|
| **定稿**（REC=1, 无上界, τ=(0.55,0.42), LEVEL=1, BANDS=3, 分组 span7） | **13,630,568** | — |
| 关水平校正（LEVEL=0） | 13,710,993 | **+80,425** |
| 用 6:00+12:00 档（BANDS=2） | 13,685,055 | +54,487 |
| 只用 6:00 档（BANDS=1） | 14,145,514 | +514,946 |
| 不用调整通道（BANDS=0） | 14,690,130 | **+1,059,562** |
| 关低需求日分组（K=4 同星期几） | 14,241,014 | **+610,446** |

**调整通道三段分解**（严格可加）：6:00 档 **544,616** ｜ 12:00 档 **460,459** ｜ 18:00 档 **54,487** ＝ **1,059,562**。

> **⚠ 18:00 档值 54,487 元（0.40%）**，与附件 3 的预报质量矩阵方向一致
> （18:00 档对净需求几乎无增量信息）。**论文如实交代，不必为"三档全用"辩护。**
> 但它比确定性计划时代的 3,865 元**高一个量级** —— 追索计划层把前两档能吃的误差吃掉了更多，
> 留给 18:00 档的份额反而变大。
>
> ⚠ **调整通道总价值 1,289,323 → 1,059,562；水平校正 132,972 → 80,425。**
> 因为**追索计划层与调整通道互为部分替代品**（计划层自己就对冲了一部分）。
> **引用 1,289,323 / 132,972 / 391,377 时必须注明它们属于确定性计划层口径。**

### 5.6 验证与自检

| 检查 | 实测 |
|---|---|
| 违约费 = 0（推论 1） | 0.00 元 ✓ |
| ≥ 完美预见下界 12,229,461（无上界同口径） | 13,630,568 ✓ |
| 储能能量守恒 `d/η ÷ c` | 0.8101 = η² ✓ |
| 真实 SOC 零越界 | 打满 [1200, 10800]、0 越界 ✓ |
| 调整层不泄漏到第 0 块 | 0 kWh ✓ |
| 写盘费 = `run()` 结算 | 差 **−0.0000 元** ✓ |

**独立复核** `代码/诊断/audit_p3_v3.py`（**不 import 任何求解器**，自己从附件 1/2 重读原始数据、
再把交付 xlsx 读回来重算）：槽位对齐 ✓；**124 个无紧急购电日的费用精确对账，最大残差 0.005225 元** ✓；
SOC 逐日递推残差 0.0003 kWh、跨日衔接 0.0000 ✓；全年合计 **13,630,567.82 元** ✓
（Σĝ 20,587,054.23 ｜ Σg 21,411,742.86 ｜ 充 6,629,037.68 ｜ 放 5,370,111.58）。
**⇒ 交付文件可被第三方按同一口径独立核出。**

> ⚠ **该脚本 E 项的区间上下界只覆盖「日界序列」**（`充放电量` 表的 0:00 / 24:00 两列，
> 得 [5,118.4, 9,791.4]），**不覆盖日内轨迹**。§5.4 报的 [1,200, 10,800] 是**日内峰值**，
> 需自行按 4 小时块重建才可核。**本版已独立重建过**：从 `result3.xlsx` 的 6 块/日充放电量
> 逐日递推（起点取 0:00 列，`ΔSOC = 0.9c − d/0.9`），得
> **区间恰为 [1200.00, 10800.00]、越界 0.00、334 天末端残差最大 0.0003 kWh** ✓。
> **两条都不许省**：日界序列看着电池没循环，日内轨迹才是可行性证据。

### 5.7 ⚠ 购电上界是**读法选择** + 对问题 2 的对照

**题面（附录 1）只给了储能「最大充放电功率 5000 kW」，没给微网与外网的联络线/购电功率上限。**
v3 缺省取 `ĝ ≤ 5000 kW` 是**自加假设**；**本版改按题面字面读法放开**（`P3_GMAX=20000`，实测峰值 10,054 kW，不触发）。

**放开不是免费的论证，是实测选出来的。四象限（每格各自调平 τ，span7）：**

| 计划层 ＼ `G_MAX` | `G ≤ 5000`（保守） | `G` 无上界（字面，**本版交付**） |
|---|---:|---:|
| 确定性计划（`problem3_v3.py`） | 14,285,731（τ'=0.55） | 14,405,793（τ'=0.75，τ₀=0.90） |
| **追索计划（定稿）** | 13,919,466（τ'=0.30） | **13,630,568（τ'=0.42）** |

1. **追索计划在两种读法下都优于确定性计划**（−366,265 / −775,225）—— **这才是本轮的主要增益。**
2. **「放开上界」只在追索计划下有利**；确定性计划下设上界反而更好 ——
   无上界时日前 LP 把电池**过度投入价格套利**，紧急购电涨到 **9.8～24.4 万 kWh**（套利挤掉可靠性）。
3. ⇒ **旧说法「追索 + 无上界必须一起开才有效」已作废** —— 那是其余三格未调平 τ 时的比较。
4. **保守读法下问题 3 也可交付了**（13,919,466），比 v3 定稿的 14,285,731 **低 366,265 元（−2.56%）**。
   ⇒ **换计划层的收益与读法无关；「放开上界」是叠加在上面的第二笔收益**（本版交付合计比 v3 低 655,163 元）。

**两条哨兵（完美预见下界）**：

| 下界 | 数值 | 说明 |
|---|---:|---|
| 无上界读法（**本版交付口径**） | **12,229,461 元** | 购电 20,187,649 kWh ｜ 零紧急 |
| `G ≤ 5000` 共同口径 | **12,406,053 元** | 购电 20,130,645 kWh ｜ 零紧急 |

`problem2.py:237` 把购电变量建成 `(0, None)`，其哨兵 LP 解出 **g_max = 10,325.33 kW**（物理上限的两倍），
故问题 2 自报的 12,229,461 **隐含无上界假设**；加回 `G ≤ 5000` 后同一条 LP 给出 **12,406,052.74 元**，
与 `problem3_recourse.sentinel()` 在 `P3_GMAX=5000` 下解出的 **12,406,053 元精确到分相同**
（两个独立写的 LP 同一结果，强交叉验证）。证据脚本 `代码/诊断/diag_sentinel_p2p3.py`。

**除总费以外的对照维度（两行都取无上界读法，可比）**：

| | 问题 2（13,846,553） | 问题 3（13,630,568） |
|---|---|---|
| 账本结构 | `p·ĝ + 5p·e`（无调整通道） | `p·ĝ + 1.5p·(g−ĝ)⁺ + 5p·e`（违约项实测 0） |
| 计划层 | 追索 | 追索 |
| 购电功率峰值（计划 `ĝ` ／ 48,096 槽） | 10,325.3 kW ｜ **7,313 槽超 5000** | 10,054.0 kW ｜ 6,877 槽超 5000 |
| 购电功率峰值（实际执行 `g`） | 同 `ĝ` | 10,241.9 kW ｜ 7,420 槽超 5000 |
| 紧急购电量 | 105,864 kWh | **49,632 kWh** |
| 高出同口径哨兵 | +13.22% | **+11.46%** |

> **⚠ 论文必须同时交代三点**：① **两条哨兵不可并列** —— 跨问对照必须统一取**同一条**；
> ② 两问总费是**各自题面口径**下的数（问题 2 无调整通道），这是**同族账本**的比较，
> **不是同一账本** —— 不要写成"同一模型下的改进"；
> ③ **必须给读法分层 + 各自调平 τ，不要给笼统的优劣排序。**

> **⚠⚠ 旧版本节写错了，已整节重写。** 旧版把「问题 2 无上界数 14,341,723」直接比「问题 3 有上界数」，
> **`G_MAX` 不匹配**；且「放开更贵 14,708,919 / 紧急 26.3 万 kWh」内部自相矛盾
> （1.9 万 kWh 的紧急缺口不可能只值 17 元差值），**正确值 14,708,902 / 243,780 kWh**。
> 正确做法是**四象限 + 每格各自调平 τ**。

### 5.8 与 v2 / v3 的对照（口径 A 下）

| 方案 | 口径 | `G_MAX` | 计划层 | 报出 | 紧急 kWh |
|---|---|---|---:|---:|---:|
| `problem3_v2.py`（K=20 + 下限，Q=0.80/0.74） | B | — | 确定性 | 14,722,577（同轨迹 A 14,908,236） | 242,484 |
| v3 原稿（无水平校正、旧基线） | A | ≤5000 | 确定性 | 14,990,108 | 148,649 |
| v3 中间稿（水平校正、旧基线 K=4） | A | ≤5000 | 确定性 | 14,712,825 | 95,749 |
| v3 定稿（+ 分组 span7） | A | ≤5000 | 确定性 | 14,285,731 | 76,784 |
| **本版定稿** | **A** | **无上界** | **追索** | **13,630,568** | **49,632** |

口径 A 下本版比 v2 同轨迹低 **1,277,668 元**；比 v3 定稿低 **655,163 元**。
紧急购电 242,484 → **49,632 kWh**（**降 79.5%**）。

> ⚠ 前四行的 `G_MAX` 读法与最后一行**不同**，前四行之间可比，与最后一行只能按 §5.7 的四象限分层读。
> 若要同读法比：保守读法下本版 13,919,466，比 v3 定稿低 366,265（−2.56%）。

> v2 时代的结论**大部分仍成立**，只是换了账本：① 三处缺陷诊断（0:00 零对冲 / 日内 LP 无紧急变量 / K 太小）；
> ② **"调整通道不增值"是 K=4 假象、已作废** —— 改成追索计划层后调整通道值 **1,059,562 元**（见 §5.5）；
> ③ 附件 3 预报质量矩阵（6:00/12:00 有增量、**18:00 档无增量**）；④ 插值权重整点 bug。
> ⑤ 「紧急购电在问题 3 中存在」的依据链不变。
> ⑥ 新增：**τ 的最优位置随计划层移动**（见 §5.5），v2/v3 时代的 τ 结论只在确定性计划层内有效。

**"是否需要引入其他时刻预报"的定量回答**（净需求 = 负荷−光伏 的预报 RMSE，kW）：

| 发布档 | 0-6h | 6-12h | 12-18h | 18-24h |
| --- | ---: | ---: | ---: | ---: |
| 0:00 | **250.7** | 538.4 | 659.1 | 305.9 |
| 6:00 | — | **343.3** | 472.2 | 305.8 |
| 12:00 | — | — | **328.9** | 305.8 |
| 18:00 | — | — | — | **301.6** |

**6:00 档把 6-12h 误差从 538 降到 343（−36%），12:00 档把 12-18h 从 659 降到 329（−50%），
但 18:00 档对 18-24h 没有增量（301.6 vs 305.9）**。⇒ 需要引入 6:00、12:00 两档；18:00 档可省。

**关键 bug 备忘**：插值权重矩阵在整点 `lo==hi` 时先赋 1 再被覆盖成 0，会把正午光伏峰值清零 → 预报总量低 18% → 计划过度购电。已修复。

**时间口径**：附件 1 的 144 槽按**结束时刻**标号，故写盘时第 k 列装 `arr[(k+1) % N]`。
这是**标签对齐，不是错位**（`problem2.py`、`problem3_v2.py` 同约定）；读回时 `np.roll(行, 1)`。

## 6. 问题 4 — 已完成（2026-09-11，定稿 `代码/problem4_3.py` + `problem4_2.py`）

**完整归档见 `文档/问题4_求解归档.md`**（本文只记要点与教训）。

> ## ✅ 2026-09-12：已完成换基线移植，`result4-*` 已重出（下方历史警告框作废）
>
> 问题 3 采纳低需求日分组（`result3.xlsx` = 14,285,731 元）后，曾发现 `problem4_3.py`
> 冻结的是**换基线之前**的 problem3 模型（`K_PEERS = 4`、无分组旋钮），两份交付物不再
> 逐元素相同。**现已把分组移植进两个 problem4 脚本并重出交付表**：
>
> | 脚本 | 改动 | 新口径 |
> |---|---|---|
> | `problem4_3.py` | `L_base` 块（382–388 行）由「同星期几 K=4」改为**低需求日分组** | span **7**（对齐问题 3） |
> | `problem4_2.py` | `peers` 行（138 行）改为**低需求日分组** + 补 `low_demand_dows` | span **6**（对齐问题 2） |
> | `problem2.py` | `GROUP_SPAN` **14 → 6**（补扫后改，见 ⑤） | span 6 |
>
> 残差那一路不用动：`problem4_3.py:792` 的 `net_hist` 本来就是逐日累积的**全历史**池，
> 等价于问题 3 定稿的 `P3_HSRC="all"`。
>
> ### 交付物新旧对照
>
> | 交付物 | 旧 | 新 | Δ |
> |---|---:|---:|---:|
> | `结果/result2.xlsx` | 14,341,722.61 | **13,846,553.27** | −495,169（−3.45%） |
> | `结果/result4-2.xlsx` | 16,050,426.66 | **14,736,352.63** | −1,314,074（−8.19%） |
> | `结果/result4-3.xlsx` | 15,443,245.19 | **15,007,661.06** | −435,584（−2.82%） |
> | `结果/result3.xlsx` | 14,285,731.48 | **不变**（问题 3 无需改，见 ⑥） | — |
>
> **① 交叉验证完全恢复**：`result4-3.xlsx` vs `result3.xlsx` —— 计划购电量、调整购电量、
> 充放电量、紧急购电量**逐元素相异 0**（槽位 max|差| 0.0000；紧急购电表 456 行同序、
> 合计 76,783.77 kWh 相同），两表购电量合计完全相同。唯一差异是**全天购电费列**：
> 14,285,731.48 → 15,007,661.06（**×1.050535**）。
> ⟹ §6.1 的「**波动电价不改变最优策略，只改变账单**」在新基线下**重新成立**。
>
> ⚠ **比对方法两个坑**（上次误判就出在这里）：① 两张表列序**相同**，比较时**两边都不许
> 单方面 `np.roll`** —— 给 result3 单边 roll 会假报「相异 17,315 格」；② `调整购电量`
> 数值区里混着字符串单元格（如 `0:00-4:00`），取数值区须 `to_numeric(errors='coerce')`。
> 另外 `ĝ` 表内单位是 **kWh/槽**（不是 kW），算 Σp·ĝ 时**不要再乘 Δt**。
>
> **② `result4-3` 的数变化**：
>
> | 项 | 旧基线 | 分组 span7 | Δ |
> |---|---:|---:|---:|
> | 总费 | 15,443,245 | **15,007,661** | −435,584 |
> | 计划 `p·ĝ` | 13,881,917 | 13,902,153 | +20,236 |
> | 超额费 | 1,072,359 | 720,462 | −351,897 |
> | 紧急费 / kWh | 488,970 / 95,749 | 385,046 / 76,784 | −103,923 / −18,965 |
> | Σĝ / Σg | 20,792,179 / 21,643,427 | 20,795,914 / 21,424,459 | +3,735 / −218,967 |
> | 丢弃购电占比 | 12.23% | **11.21%** | −1.02 pp |
> | 贴购电上界槽占比 | 36.40% | **35.40%** | −1.00 pp |
> | **账单倍数 vs 问题 3** | ×1.049645 | **×1.050535** | — |
> | **哨兵** | 13,014,628 | **13,014,628（不变）** | 0 |
> | 高出哨兵 | 18.66% | 15.31% | — |
>
> 哨兵不变是因为完美预见 LP 只用实际 `load`/`pv` 与实际价，**`L_base` 根本不进模型**。
> 以上各数与移植前猴补丁预演的结果**完全一致** ⟹ 预演可信、移植无副作用。
>
> **③ `result4-2` 的数变化**：16,050,427 → **14,736,353**（−1,314,074 / −8.19%），
> 计划费 14,206,059 + 余 530,293；Σg **21,637,974 kWh**；丢弃购电 16.39% → **12.41%**；
> 贴购电上界槽占比 **34.54%**。哨兵 13,014,628 不变。

> ## ⚠⚠ 2026-09-12 第二次失效：本节（以及 §6.1–§6.4 的 4-3 列）**又过期了**
>
> 本次问题 3 的基准**第二次改变**：计划层改**追索变量** + 购电改**无上界读法** + **τ 重调为 (0.55, 0.42)**。
> `结果/result3.xlsx` 已从 14,285,731 换成 **13,630,568**，而 `代码/problem4_3.py` 仍按**旧基准**（确定性计划层、`G≤5000`）移植。
>
> **⟹ 后果**：
> 1. **§6.1 的交叉验证断言「result4-3 与 result3 逐槽相异 0 / 48,096」不再成立** —— 两份文件的策略表现在不同。
> 2. §6.2 / §6.4 里 4-3 列的绝对值（15,007,661、丢弃 11.21%、贴界 35.40%）**基于旧基准**，
>    与现行 `result3.xlsx` 不同源。**只保留方向性结论**：波动电价不改策略只改账单、
>    丢弃购电是继承自问题 3 的性质、两条负结果方向不翻转。
> 3. §6.1 里「口径矩阵」一节的迁移前 K=4 数值标注仍然有效。
>
> **⟹ 本轮不动**（用户明确要求本轮只落数据与归档）。**登记为待办：见 §7 第 9 条。**
> 论文若要用问题 4 的绝对数，**必须先让 `problem4_3.py` 跟到新基准再重出 `result4-3.xlsx`。**

> **④ 问题 4 的 9 条内部结论方向全部不翻转**（含 `WQ=1` 在 `G=∞` 下反号那条例外）：
> 读法A 更省、`G=∞` 更贵、τ=0.836 更贵、BANDS=2 更贵、`WQ=1` 更贵、LEVEL=0 更贵、ADJ=0 更贵。
> 只是**幅度普遍缩小**（如 LEVEL 的估值 411,871 → 147,055，与问题 3 里「分组与水平校正
> 互为部分替代品」完全同源）。
>
> **⑤ ★ 问题 2 的 `GROUP_SPAN` 补扫（14 臂，本次新做）**：
> `problem2.py` 旧定稿 `GROUP_SPAN = 14` **从未验证过**（归档 §7.2 五臂里没有窗口变体）。
> 扫描结果（γ=1 可交付 + 因果执行，334 个计费日）：
>
> | span | 总费（元） | vs span14 | 判定 |
> |---|---:|---:|---|
> | 4 / 5 | 13,621,172 / 13,622,447 | — | ⚠ **作废：前视污染**（见下） |
> | **6** | **13,846,553** | **−495,169** | ✓ 干净，**本次采用** |
> | 7 | 13,870,178 | −471,544 | ✓ 干净 |
> | 8 / 10 | 13,957,388 / 14,067,328 | −384,335 / −274,395 | ✓ 干净 |
> | **14（旧定稿）** | **14,341,723** | — | ✓ 干净 |
> | 21 / 28 | 14,713,463 / 15,032,496 | +371,740 / +690,773 | ✓ 干净 |
>
> - **成本在可行域上单调递增 ⟹ 越短越好**；可行最优 = 可行下界 = **6**。
> - **为什么下界恰好是 6**：低需求日 {Fri,Sat} 每周只有 2 天，回看 s 天只能捞到 ≈2s/7 个
>   同组日；s=6 时周五/周六各捞到 1 个，而 **s≤5 时窗口（周日…周四）里一个同组日都没有**，
>   `sc_of` 退化成 `[d]` 自身 —— **拿当天实际负荷冒充情景天，是前视作弊**。
>   纯 numpy 核验：span 4/5 在计费窗口内各有 **47 天**踩此回退（平均情景数 1.99 / 2.41），
>   span 6/7/8/14 均为 **0 天**（3.12 / 4.10 / 4.79 / 8.11）。
> - **诚实边界**：6 是**约束下的最优**，不是无约束最优 —— 真实最低点可能更短，
>   但那片区域被前视污染、测不了。**论文必须写明这一点。**
> - 旧基线 K=4 复现为 15,134,192 ✓、`全七天 span14` 复现为 17,099,172 ✓（篮子可信）。
>
> **⑥ ★ 问题 3 不跟着改成 6 —— 已实测，改了更贵**：同分组下跑 `problem3_v3.py`（`P3_GSPAN`）：
>
> | | span 6 | span 7（定稿） |
> |---|---:|---:|
> | 总费 | 14,317,826 | **14,285,731** ✓ |
> | 计划费 | 13,223,433 | 13,195,519 |
> | 超额费 | 641,346 | 668,682 |
> | 紧急费 | 453,047 | 421,530 |
>
> span 7 精确复现交付值 14,285,731 ✓。span 6 **贵 32,095 元**：计划层多买了 27,914 元的电，
> 紧急购电反而**更多**（+21,517）—— **买的量够了，形状不对**，因为低需求日在 span 6 下
> 只剩 **1 个**样本，情景均线形状失真。
> ⟹ **问题 2 偏好 6、问题 3 偏好 7，两者方向相反**；这正说明「P2 的规律不能外推到 P3」
> 不是托词。两问窗口不一致**不构成错误**：各有各的模型、各扫各的参数、各自取到最优，
> 论文注明一句即可。

### 6.1 ★★ 核心结论：波动电价不改变最优策略，只改变账单

`结果/result4-3.xlsx` 与 `结果/result3.xlsx` 的**策略表逐元素相同**
（计划/调整购电量**逐槽相异 0 / 48,096**、充放电量 **2,004 行全等**、紧急购电量
**456 行同序全等**），唯一差异是**全天购电费列**：
14,285,731.48 → **15,007,661.06 元（×1.050535）**，逐日比值 0.7626–1.1864、334/334 天全变
（费用列含紧急项，故**不是**恒定倍数）。

> ⚠⚠ **本条现已失效（2026-09-12 第二次换基准，见 §6 开头）。** 上式左右两侧的
> `result3.xlsx` 已换成 13,630,568，故「逐槽相异 0」**不再成立** ——
> 这是**待办**（§7 第 9 条），不是结论。**论文引用本条前必须先重出 `result4-3.xlsx`。**
>
> 历史（供追溯）：14,712,825.34 → 15,443,245.19（×1.0496，K=4 旧基线，作废）；
> 14,285,731.48 → 15,007,661.06（×1.050535，分组基线，**现亦作废**）。
> **机制结论本身不受影响**：波动电价不改策略、只改账单 —— 这一点有±30% 价格缩放的 13 档线性扫描佐证。

由两份独立交付物交叉验证，**不是推理**。另一佐证：价格缩放 ±30%（13 档）下
策略**逐槽不变**、账单**精确线性**（`max|total(s) − s·total(1)| = 0.000000 元`）。

### 6.2 结果（窗口 2/1–12/31，334 天）

> ⚠ **4-3 列已过期**（问题 3 第二次换基准，见 §6 开头）：15,007,661 / 720,462 / 385,046 / 35.40% / 11.21%
> 均基于**旧基准**（确定性计划层 + `G≤5000`）。**待办 §7 第 9 条。** 4-2 列未受影响。

| 项 | result4-3（问题3 对应）⚠ 过期 | result4-2（问题2 对应） |
|---|---:|---:|
| **总费用** | 15,007,661 元（旧基准） | **14,736,353 元** |
| 计划 `p·ĝ` ／ 违约 ／ 超额 ／ 紧急 | 13,902,153 ／ **0** ／ 720,462 ／ 385,046 元 | 14,206,059 ／ — ／ — ／ 530,293 元 |
| 哨兵（同口径） | 13,014,628 元（高出 15.31%） | 13,014,628 元（高出 13.23%） |
| 贴购电上界槽占比 | 35.40% | 34.54% |
| ⚠ 丢弃购电 | **11.21%** | **12.41%** |

**口径矩阵**（`diag_p4_readings.py`，⚠ 下表为**移植前 K=4 基线**的数，仅相对结论可引用）：
读法 A 15,389,306（价格信息只值 **53,940 元 / 0.35%**）；
`G=∞` 15,856,122（**放开更贵 +412,877 元，方向与问题 3 一致，方案阶段预留的"可能翻转"未发生**）；
`WQ=1` 15,455,826（**负结果**，故缺省关闭）。

### 6.3 两条负结果（都要写进论文，别藏）

1. **τ 重扫：理论预期的 0.836 位移没有出现。** 0.80 仍是实测最优，0.836 反而更贵。
   原因：与缺口正相关的只是日水平因子那 9.2% 方差，而均匀平移不改变边际权衡。
2. **WQ（电价加权分位）输给不加权**。
   唯一例外是 `G=∞` 下反号，说明它与购电上界有交互 ⇒ 留作未解问题。

> ⚠ 这两条的**绝对差额是 K=4 旧基线上量的**（移植后幅度普遍缩小，见 `文档/问题4_求解归档.md`
> §5.2–§5.4 的 ⚠ 标注）；**方向不翻转**，论文只引用方向。

### 6.4 ★ 交付表 11.21%（4-3）/ 12.41%（4-2）的购电量既未供负载也未进电池

代数：令 $S = P+g+d-L$，则残差 $= \sum(c-S)\mathbb{1}[S\ge0] \equiv -\sum\max(0,S-c) \le 0$，
即**富余被无声丢弃**。（曾怀疑「缺口槽充电凭空造电」，已用 L1 证伪：缺口槽充电 0 槽。）
分窗：盲窗 11.9% ／ 6–12h **50.0%** ／ 12–18h 30.0% ／ 18–24h 8.1%。

**这是 `problem3_v3.py` 继承的性质，不是问题 4 引入的**（对 `result3.xlsx` 量出完全相同的 −2,647,465 kWh）。
论文必须声明。

### 6.5 教训（供后续复用）

- **⚠ 死参数会制造静默假阴性**：`problem4_3.py` 早期版本的 `P4_TAU` **从未接入模型**，
  只在日志里打印。扫它会得到 13 个一字不差的总费却毫无报错。**已修**（现为 `TAU_B[0]` 的别名）。
  教训：**加旋钮后必须验证旋钮真的生效**（本次诊断脚本内置了"不同取值个数 = 8/8"的自检）。
- **⚠ 哨兵不可跨 `G_MAX` 比较**：哨兵自己也受上界约束，`G=∞` 的哨兵（12,782,592）
  低于 `G≤5000` 的（13,014,628）。两张都是真下界，"高出哨兵 X%"只能在同 `G_MAX` 内比。
- **⚠ 不能拿 `max|差|` 直接判交付表一致性**：`调整购电量` 的数值区里混着**费用列**
  （每天一个值），会报出「相异 334 格」的假警。必须**按列段**分开比。
- **⚠ SOC 区间不能用日末序列**：日末区间 [2,830, 6,000] 而真实轨迹打满 [1,200, 10,800]。
- **常数价格回归是派生脚本最有价值的自检**（`P4_PRICE_SRC=att1`）。本次回归实质通过；
  逐槽残留两类差异均为等价最优/表示精度：25 处 ±0.0001 kWh 的 4 位小数舍入边界效应，
  以及**第 292 天一处 LP 退化多解**（槽 138↔140 对调 48.3786 kWh，同属 23:00–24:00
  同一电价档，故费用分毫不差）。

### 6.6 对复核报告 §5 第 10 条（文献 3）的回应

文献 3：动态电价可提高收益 28%，但成本 +94%、EFC 上升致盈利下降。
**本模型不矛盾但落点不同**：① 题面不售电，**没有收益侧**；② 纯购电费用口径下
效应只有 **+4.96%** 且策略不变（文献 3 的 +94% 含退化等成本项）；
③ **本模型不计电池退化成本**（一贯取舍，须声明），故 EFC 渠道未被内化。
文献 3 的 **±30% / 5% 步长**敏感性规范**已照搬**（`diag_p4_pscale.py`，13 档）。

## 7. 下一步

1. ~~写 `问题3_求解归档.md`~~ — **已完成；2026-09-12 第二次定稿（追索计划层 + 无上界 + τ 重调）为
   口径 A / **13,630,568 元**。归档 §5.2 是本次核心：τ 随计划层移动的实测面板与机理。**
2. ~~问题 4（波动电价）：重算 result4-2 / result4-3~~ — **已完成，见 §6 与
   `文档/问题4_求解归档.md`**。⚠ **但 4-3 列已因问题 3 二次换基准而过期**，见第 9 条。
3. 论文口径表 1/2/3/4：问题 2、问题 3、问题 4、完美预见下界四栏对齐。
   **⚠ 下界栏必须标注读法**：无上界读法 = **12,229,461**（本版问题 3 交付口径、也是问题 2 自报口径）；
   `G≤5000` 共同口径 = **12,406,053**。**必须在同一读法内横向比较，不得跨读法并列。**
   **⚠ 问题 4 的哨兵 13,014,628 是"附件 4 实际价 + G≤5000"下的绝对值，与前三个问题的
   哨兵不是同一口径，四栏并列时必须注明。**
   **⚠ 问题 2 与问题 3 的对照栏必须按 `G_MAX` 读法分层 + 各自调平 τ（§5.7 四象限），不得给单一排序。**
4. 论文里必须如实交代的（勿漏）：
   - **口径 A 是用户指定**，且与 v2 的口径 B **不可混比**；同一轨迹两口径差 `Σp·(ĝ−g)⁺`。
   - **★★ τ 必须随计划层重调**（本轮最重要的方法学发现，§5.5）：
     确定性计划时代的 `(0.80, 0.55)` 搬到追索版**偏贵 185,974 元**；重调后 `(0.55, 0.42)` 内点最优。
     **旧断言「τ 的最优位置不随配置移动」已作废。** 且 τ **只能扫、不能推**
     （静态理论给 0.80 与 1/3，两个都被实测推翻 —— 因为追索计划层的 c/d 本身就逐情景对冲）。
   - **调整通道值随计划层变**：确定性基线 **1,289,323** → 追索基线 **1,059,562**；水平校正 132,972 → 80,425。
     **旧值仍成立，但引用时必须注明基线口径**（追索计划层与调整通道互为部分替代品）。
     **"调整通道几乎不增值"的 K=4 旧结论仍作废。**
   - **18:00 档增值很小**：追索基线 **54,487 元 / 0.40%**（确定性基线 3,865 元 / 0.027%；
     旧基线 26,743 元 / 0.18%）—— 方向一致（附件 3 说 18:00 档无增量信息），如实交代。
   - **购电上界是读法选择，不是优化结论**：题面只给储能 5000 kW。本版按**字面读法放开**
     （`P3_GMAX=20000`，峰值 10,054 kW）；**放开只在追索计划下有利**，确定性计划下设上界更好
     （§5.7 四象限）。**必须给读法分层，不得给单一排序。**
   - **τ 是平台中心不是尖点最优**（`τ₀∈[0.50,0.55] × τ'∈[0.42,0.44]` 极差 2,296 元 / 0.017%）。
   - **两条哨兵不可并列**：12,229,461 只配"无上界"读法，共同口径是 12,406,053。
   - **问题 3 的 `L_base` 低需求日分组**（§5.3）口径与无前视依据；
     **`L_base` 分组有用、残差分位分组显著有害（p<0.001）** —— 两处口径有意不一致，必须写清；
     **分组与水平校正互为部分替代品，两个增益不可相加**；
     **`P3_GRP` 已是脚本缺省，复现旧口径须显式 `P3_GRP=''`**。
   - **问题 4**：交付表 11.21% / 12.41% 的丢弃购电（**4-3 列待重出，见第 9 条**）；
     不建模电池退化成本（§6.6）；τ' 实测最优 0.50 但为可比性保留 0.55。
   - **问题 2 的回看窗口 `GROUP_SPAN` 经过 14 臂扫描才定稿为 6**，
     且 **6 是「不作弊前提下能取的最短窗口」**（s ≤ 5 时 `sc_of` 会用当天负荷冒充情景天、
     属前视作弊，共 47 天）。**这是约束下的最优，不是无约束最优**（§6 ⑤）。
   - **问题 2 与问题 3 的回看窗口有意不一致（6 vs 7）**，且实测方向相反
     （P3 改 6 会贵 10,721 元，追索基线下）。跨问对照时须注明各自窗口（§6 ⑥、§5.5）。
     ⚠ 注意：确定性基线下这条差值是 32,095 元，**追索基线下缩小到 10,721 元**。
5. ~~问题 4 要不要跟着问题 3 换基线~~ — **已完成（2026-09-12）：采用选项 (a)「移植并重出」。**
   分组已移植进 `problem4_3.py`（`L_base` 块，span **7** 对齐问题 3）与 `problem4_2.py`
   （`peers` 行 + `low_demand_dows`，span **6** 对齐问题 2）。新交付值
   `result4-3.xlsx` = **15,007,661.06**、`result4-2.xlsx` = **14,736,352.63**。
   9 条内部结论方向全不翻转。**见 §6 ①②③④。**
   ⚠ **该移植现在又过期了 —— 见第 9 条。**
6. ~~问题 2 要不要补一次 `GROUP_SPAN` 扫描~~ — **已完成（2026-09-12）：已扫 14 臂，改定为 span 6。**
   成本在可行域（s ≥ 6）上单调递增，故可行最优 = 可行下界 = **6**（s ≤ 5 会触发 `sc_of`
   前视回退、作废）。`problem2.py: GROUP_SPAN` 14 → **6**，`result2.xlsx` 重出为
   **13,846,553.27**（越限槽数同时从 7,801 降到 **7,313**）。**问题 3 保持 span 7**。
   完整扫描表见 §6 ⑤。
7. 决定 result3.xlsx 里「调整购电量」写最终调整量 g 还是净调整量（现按题面写 g）。**已完成：写最终执行量 g。**
8. ~~`代码/make_tables.py:99-104` 的 `tables_p1()` 在 `结果/result1.xlsx` 上抛 `KeyError`~~
   — **已修复（commit `fd646c9`）**：result1 表头是旋转的，须按标签解析。
   现已验证 `python 代码/make_tables.py 1|2|3` 三问全部跑通（注意**必须带参数**，
   无参调用会在 `make_tables.py:298` 抛 `IndexError`）。
   ⚠ **但 `make_tables.py` 的问题 3 汇总表仍是按旧 `result3.xlsx` 生成的**，
   本轮换数后需重跑 `python 代码/make_tables.py 3` 并提交（见第 10 条）。
9. **★ 待办：`代码/problem4_3.py` 需再跟一轮新基准（本轮用户明确不动）。**
   问题 3 的基准在 2026-09-12 第二次改变：计划层 → **追索变量**、购电 → **无上界读法**、
   τ → **(0.55, 0.42)**，交付值 14,285,731 → **13,630,568**。
   `problem4_3.py` 当前的移植只跟到了第一次（分组 span7），**策略现在与 `result3.xlsx` 不同源**：
   - `文档/问题4_求解归档.md` 与 §6 里 4-3 列的绝对值（15,007,661 / 丢弃 11.21% / 贴界 35.40%）**过期**；
   - **§6.1 的交叉验证断言「与 result3 逐槽相异 0」不再成立**；
   - 方向性结论（波动电价不改策略只改账单、丢弃购电是继承性质、两条负结果）**仍成立**。
   重做时需要同步：`problem4_3.py` 的计划层改追索、`G_MAX` 读法、τ 取值，然后重出
   `结果/result4-3.xlsx` 与 `文档/问题4_求解归档.md`，并恢复与 `result3.xlsx` 的逐元素交叉验证。
10. **本轮收尾**：`python 代码/run_problem3.py` 重出 `结果/result3.xlsx`（13,630,568）；
    `python 代码/make_tables.py 3` 重出问题 3 表；`python 代码/诊断/audit_p3_v3.py` 独立复核通过。

## 8. 文件结构

```
数模国赛C题/
├── 代码/  problem1.py, problem2.py(★问题2唯一求解器),
│          run_problem3.py(★★问题3**交付入口**：钉死四个旋钮，直接出 结果/result3.xlsx),
│          problem3_recourse.py(★★问题3**定稿求解器**：追索计划层，口径 A，
│                               不 import 队伍求解器；带「是否交付配置」自检打印),
│          problem3_v3.py(确定性计划层版本，**仅作 §5.7 四象限对照**，封面已标注弃用指向),
│          problem3.py(更旧的版本，仅历史留痕),
│          problem4_2.py(★问题4-2 求解器), problem4_3.py(★问题4-3 求解器，⚠ 待跟新基准),
│          problem3_v2.py(口径B，对照保留), p3v2_ablation.py, p3v2_sweep.py,
│          p3_stoch.py(对照实现，勿删),
│          make_tables.py(由 结果/*.xlsx 生成论文表；**必须带参数** `python 代码/make_tables.py 3`),
│          独立视角/(独立复核的复算脚本，不 import 队伍求解器),
│          诊断/(diag_*.py、audit_p3_v3.py(独立复核交付文件)、
│                diag_sentinel_p2p3.py(两条哨兵的口径差)、
│                _p3_premise.py / _p3_group_exp.py / _p3_group_exp2.py /
│                _p3_resid.py / _p3_mech.py / _p3_recency.py / _p3_tau_scan2.py
│                (★问题3分组证据链，§5.3；_p3arms/*.npz 按 .gitignore 忽略)、
│                _pr1/(问题2独立复核，已入库)、
│                diag_p4_price.py / diag_p4_audit.py / diag_p4_readings.py /
│                diag_p4_tau.py / diag_p4_pscale.py — 保留备查)
├── 结果/  result1.xlsx, result2.xlsx, result3.xlsx,
│          result4-2.xlsx, result4-3.xlsx
├── 文档/  问题1_求解归档.md, 问题2_求解归档.md, 问题2_独立复核_可交付性与追索模型.md,
│          问题3_求解归档.md(★问题3 权威归档), 问题4_求解归档.md, 复核报告.md, taskrun.md
├── 追索无上界/  追索无上界_结果.md(**已被取代，仅留配置来源痕迹**；
│              原 result3_recourse_unbounded.xlsx 已删除，避免与交付值混淆)
└── 附件/  附件1-4.xlsx, 附件5/(result 模板，含 result4-2/result4-3 模板)
```

> **2026-09-12 问题 3 二次定稿的文件变动**：
> - 新增 `代码/problem3_recourse.py`（由 `追索无上界/problem3_v3_recourse.py` 升格、改名）
>   与 `代码/run_problem3.py`（由 `追索无上界/run_recourse_unbounded.py` 升格）；
> - 删除 `追索无上界/result3_recourse_unbounded.xlsx`（旧 τ 的 720 KB 重复工作簿，
>   留着有被误读为交付值的风险，可用 `P3_TAUB=0.80,0.55,0.55,0.55 P3_TAUP=0.55` 随时重生成）；
> - `代码/problem3_v3.py` 降为对照，封面加弃用指向。

> **2026-09-11 清理**：删除了问题 2 的废弃方案与其论证文档 ——
> `problem2_stochastic.py`（d̂ 当承诺量，报出低于自身物理下界的数）、`problem2_quantile.py`
> （临界比 `5p/(5p+p)` 算错，应为 `4p/(4p+p)=0.8`）、`problem2_forecast_compare.py`（口径含 ĉ）、
> `verify_p2.py`、`diag_oracle.py`、`文档/问题2_最优性论证.md`、`文档/问题2_求解方案_建议.md`。
> 现问题 2 的唯一求解器是 `代码/problem2.py`。
