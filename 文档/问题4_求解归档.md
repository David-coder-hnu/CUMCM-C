# 问题 4 求解与归档文档（波动电价下的当天购电策略）

> **定稿**：`代码/problem4_3.py`（对应问题 3）、`代码/problem4_2.py`（对应问题 2）
> **交付** `结果/result4-3.xlsx`（**15,443,245 元**）、`结果/result4-2.xlsx`（**16,050,427 元**）
> 计费窗口 2025.2.1–12.31（334 天，1 月热身）｜ 口径 A ｜ 读法 B（主模型）｜ `G ≤ 5000 kW`
>
> **★ 核心结论：波动电价不改变最优策略，只改变账单。**
> `结果/result4-3.xlsx` 与问题 3 的 `结果/result3.xlsx` **策略表逐元素相同**
> （计划/调整购电量的逐槽值、充放电量、紧急购电量全部一致），唯一差异是费用列：
> 14,712,825.34 → 15,443,245.19 元（**×1.0496**）。这不是推理，是两份独立交付物的交叉验证（§6.1）。
>
> `problem2.py` / `problem3_v3.py` / `result1-3.xlsx` **一字未动**，旧结果随时可复现。

---

## 0. 结论速览

| 项 | result4-3（对应问题 3） | result4-2（对应问题 2） |
|---|---:|---:|
| **总费用** | **15,443,245 元** | **16,050,427 元** |
| 　计划购电费 `Σp·ĝ` | 13,881,917 元 | 14,751,895 元 |
| 　违约费 `0.5p·(ĝ−g)⁺` | **0 元**（口径 A 推论 1） | **0 元**（同左） |
| 　超额费 `1.5p·(g−ĝ)⁺` | 1,072,359 元 | （并入上项口径） |
| 　紧急购电费 `5p·e` | 488,970 元 ｜ 95,749 kWh | 1,298,532 元 ｜ 206,893 kWh |
| 计划购电量 `Σĝ` | 20,792,179 kWh | 22,444,160 kWh |
| 实际购电量 `Σg` | 21,643,427 kWh | （同上，见 §7.1） |
| 储能充／放 | 6,069,324 ／ 4,916,804 kWh | 5,435,306 ／ 4,402,435 kWh |
| 真实 SOC 区间 | **[1,200, 10,800] kWh**（零越界） | **[1,200, 10,800] kWh**（零越界） |
| 年末 SOC | 5,276 kWh | 6,713 kWh |
| 完美预见下界（哨兵，同口径） | 13,014,628 元 ｜ 高出 18.66% | —（见 §5.5） |
| 贴购电上界的槽占比 | 36.40% | 36.61% |
| ⚠ 买了但既未供负载也未进电池 | **12.23%**（2,647,466 kWh） | **16.39%**（3,678,992 kWh） |

**问题 4 相对问题 3 的三个数**（同一窗口、同口径只换价格）：

| 量 | 值 | 含义 |
|---|---:|---|
| 账单变化 | **+730,420 元（×1.0496）** | 波动电价对成本的**全部**影响 |
| 策略变化 | **0**（逐元素相同） | 波动电价对决策的影响 |
| 价格信息的价值 | **53,940 元（0.35%）** | 0:00 是否已知全天电价值多少（读法 B − 读法 A） |

---

## 1. 价格口径：三层分工

### 1.1 附件 4 不是独立的价格序列

诊断 `代码/诊断/diag_p4_price.py` 的三条实测（`_p4_price_diag.txt`）：

- **附件 4 的逐槽跨日均值剖面与附件 1 基准剖面的最大绝对差 = `5.151e-05` 元。**
  即 **附件 1 就是「典型日分时电价」形状，附件 4 = 该形状 + 波动**。
- **方差分解**：总方差 0.116056 ｜ 日内形状 **84.6%** ｜ 日水平因子 9.2% ｜ 日内残差 6.2%。
  日内形状近乎确定（常数基准剖面与真值的相关已达 0.971），而日水平因子 $a_d$ 是**全天均匀平移**。
- 日均价 mean 0.7662、std 0.1036、min 0.5378、max 0.9290（**std/mean = 13.5%**）。

**为什么这三条决定了整个建模路线**：口径 A 的四项费用 `p·ĝ`、`0.5p`、`1.5p`、`5p`
**都是同一个 $p$ 的倍数**。均匀平移不改变任何边际权衡 ⇒ 价格的水平信息对决策几乎无用。
可判断的那部分（日水平因子）又恰好是最不重要的那部分。这条推理在 §4 与 §5.3 被两次实测确认。

### 1.2 两个价格向量，分工不可混用

| 向量 | 来源 | 用途 |
|---|---|---|
| `price_base` / `price` | 附件 1 公布的分时电价剖面（全天同一条） | **计划层**的价格预期（读法 B） |
| `PR` / `price_real` | 附件 4 实际结算价（逐日逐槽波动） | **结算层 + 写盘**（唯一花钱处） |

**铁律：结算与写盘永远用实际价 `PR`；计划层的系数取决于读法。** 两者混用会得到
「用公布价算钱」或「用实际价做计划」的错误模型。`problem4_3.py` 里共有 **7 个价格进入点**，
模块 docstring 逐一列出；写盘的 `assert abs(tot - r["total"]) < 1.0` 同时校验结算层与写盘层，
是防混用最有价值的一道防线。

### 1.3 两种读法

- **读法 B（主模型，`P4_READING=B`，缺省）**：0:00 **不知道**当天实际电价，只有公布的
  分时剖面作预期；6/12/18 档用当天已发生时段的 `实际价/基准剖面` 比值做水平校正。
- **读法 A（下界 + 敏感性，`P4_READING=A`）**：0:00 **已知**全天 `PR[d]`，计划层系数直接用它。

两者之差就是**价格信息的价值 = 53,940 元（0.35%）**。这个数足够小，本身就是一个结论。

### 1.4 读法 B 下的一个结构性事实

读法 B 下，`exec_day` 的滚动 MPC 窗口价、`plan_day` 的 LP 目标、哨兵以外的一切
**都退化为 `price_day`** —— 与 `problem3_v3.py` 完全相同。于是策略逐元素相同（§6.1）。
**这不是巧合，是 §1.1 那三条性质的必然结果。**

---

## 2. 模型结构：从问题 3 派生

### 2.1 不改动的部分（全部沿用）

口径 A（全额 `p·ĝ`，推论：违约费 ≡ 0）、$\gamma=1$ 可交付储能计划、334 天计费窗口、
SOC ∈ [1200, 10800]、日末回 6000、不售电、时间标签左移约定、`scipy.linprog(method="highs")`。

### 2.2 改动的部分

| 旋钮 | 缺省 | 作用 |
|---|---|---|
| `P4_READING` | `B` | 读法 A / B（§1.3） |
| `P4_GMAX` | `5000` | 购电功率上界（§7.2） |
| `P4_WQ` | `0` | 电价加权分位，**负结果，关闭**（§5.4） |
| `P4_PSCALE` | `1.0` | 实际结算价整体缩放，敏感性用（§5.3） |
| `P4_TAU` | `0.80` | 0:00 块分位（§5.2） |
| `P4_TAUP` | `0.55` | 调整层分位（§5.2） |
| `P4_PRICE_SRC` | `att4` | 置 `att1` 退回常数价 → 回归自检（§6.2） |
| `P4_OUT` | — | 写盘路径覆盖，敏感性扫描用（避免覆盖交付文件） |

### 2.3 场景集：价格与净负荷的**联合**历史样本

`problem3_v3.py` 的 K=4 档依赖附件 3 的 4 档光伏预报，与价格不联动。问题 4 需要
「缺口时段电价更高」这条渠道，故紧急购电系数从标量 `5.0 * price_day / Kd`
改为**逐场景逐槽** `5.0 * scen_price(dd, pd_) / Kd`。

**这一项就是读法 B 的全部经济内容**（`problem4_2.py:235`）——它让缺口时段的 5p 更高进入模型。
问题 4-2 的策略因此**确实**不同于 `problem2.py`（见 §4 表），这与问题 4-3 的情况相反。

**耦合实测**：日水平因子 $a_d$ 与日均净负荷 **corr = 0.982**（vs 负荷 0.898，vs 光伏 −0.343）。
即「缺电的时候电价系统性更高」。这条渠道只依赖历史联合分布，不依赖预报精度。

---

## 3. 数据与参数

储能（附录 1）：容量 12,000 kWh，最大充/放 5,000 kW，`SOC₀ = 6,000 kWh @ 2025-01-01 0:00`，
`SOC ∈ [1,200, 10,800] kWh`，充/放各打 9 折（往返 81%，放/充 = η² = 0.8101）。
离散化 144 槽/天 × 10 min（Δt = 1/6 h），共 365 天，计费窗口 2/1–12/31（334 天）。

价格预报侧的水平校正 R²（`_p4_price_diag.txt`）：**6h 0.428 ／ 12h 0.749 ／ 18h 0.837**
——与负荷侧同量级，值得开。**未新造价格预报器**：诊断已证明更好的预报器
（同星期几前 4 日均值）只值 0.02%，徒增论文解释负担。

---

## 4. 求解结果

### 表 1｜口径矩阵（`problem4_3`，读法 × 购电上界 × WQ）

| 配置 | 总费(元) | 计划 `p·ĝ` | 超额 | 紧急 | 哨兵(元) | 高出哨兵 |
|---|---:|---:|---:|---:|---:|---:|
| **读法 B  G≤5000  WQ=0（主交付）** | **15,443,245.20** | 13,881,916.56 | 1,072,358.95 | 488,969.70 | 13,014,628.45 | 18.66% |
| 读法 A  G≤5000  WQ=0（下界） | 15,389,305.63 | 13,826,355.92 | 1,070,052.99 | 492,896.72 | 13,014,628.45 | 18.25% |
| 读法 B  G=∞  WQ=0 | 15,856,122.40 | 13,232,928.06 | 975,212.19 | 1,647,982.15 | 12,782,591.81 | 24.04% |
| 读法 B  G≤5000  WQ=1 | 15,455,826.31 | 13,947,345.69 | 1,050,491.80 | 457,988.82 | 13,014,628.45 | 18.76% |
| 读法 B  G=∞  WQ=1 | 15,822,852.43 | 13,291,996.15 | 955,289.24 | 1,575,567.04 | 12,782,591.81 | 23.78% |

行间差（正数表示后者更便宜）：

| 对照 | 差值 | 解读 |
|---|---:|---|
| 价格信息的价值（B − A） | **+53,940 元（0.35%）** | 0:00 知道全天电价几乎不值钱 |
| 放开购电上界的代价（∞ − 5000） | **+412,877 元** | 上界反而是好事，方向与问题 3 一致 |
| 电价加权分位的增量（WQ=1 − WQ=0） | +12,581 元 | **负结果**，故关闭（§5.4） |
| G=∞ 下加权分位的增量 | **−33,270 元** | 该机制与上界存在交互，留作未解问题 |

> ⚠ **哨兵不可跨上界比**：哨兵自己也受 `G_MAX` 约束，故 `G=∞` 那一档的哨兵
> （12,782,591.81）低于 `G≤5000` 的哨兵（13,014,628.45）。两张都是真下界，只是约束集不同；
> 「高出哨兵 X%」**只能在同一 `G_MAX` 内横向比较**。

### 表 2｜`result4-3.xlsx` 摘要

| 项 | 值 |
|---|---:|
| 计划购电量 `Σĝ` ／ 实际购电量 `Σg` | 20,792,179 ／ 21,643,427 kWh |
| 储能充／放 | 6,069,324 ／ 4,916,804 kWh（放/充 = 0.8101 = η²） |
| 真实 SOC 日内轨迹 | **[1,200, 10,800] kWh**（打满上下限，**零越界**） |
| 日末 SOC 区间 ／ 年末 | [2,830, 6,000] ／ 5,276 kWh |
| 购电功率峰值 | 5,000.0 kW（自加上限，贴上限槽 **36.40%**） |
| ⚠ 丢弃购电 | 2,647,466 kWh = **12.23%** 购电（§7.1） |

### 表 3｜`result4-2.xlsx` 摘要

| 项 | 值 |
|---|---:|
| 计划购电量 `Σĝ` | 22,444,160 kWh |
| 计划 `p·ĝ` ／ 紧急 | 14,751,895.07 ／ 1,298,531.59 元（206,893 kWh） |
| 储能充／放 | 5,435,306 ／ 4,402,435 kWh（放/充 = 0.8100） |
| 日末 SOC 区间 ／ 年末 | [1,486, 9,363] ／ 6,713 kWh |
| 购电功率峰值 ／ 贴上限槽 | 5,000.0 kW ／ **36.61%** |
| ⚠ 丢弃购电 | 3,678,992 kWh = **16.39%** 购电（§7.1） |

**问题 4-2 与问题 2 的区别是真实的**：场景系数（§2.3）改变了策略，不是只改账单。
这与问题 4-3 的 bit-identical 形成对照，理由见 §2.3 与 §6.1。

---

## 5. 调参与消融

### 5.1 定稿配置

`P4_READING=B`、`P4_WQ=0`、`P4_GMAX=5000`、`P4_TAU=0.80`、`P4_TAUP=0.55`、
`TAU_B=[0.80, 0.55, 0.55, 0.55]`、`P4_PSCALE=1.0`。

### 5.2 分位重扫（`diag_p4_tau.py`）

问题 3 的 `τ=0.80` 是在**常数价格**下扫出来的。问题 4 下 $p$ 与缺口正相关改变了有效成本比
（$c_u = 4E[p|\text{缺口}]$、$c_o = E[p|\text{过剩}]$），**必须重扫**。纯理论估计给出最优应上移到 **≈0.836**。

**实测（0:00 块分位）**：

| τ | 总费(元) | 与 0.80 之差 |
|---:|---:|---:|
| 0.700 | 15,467,306.55 | +24,061 元 |
| 0.750 | 15,452,761.20 | +9,516 元 |
| 0.780 | 15,446,284.38 | +3,039 元 |
| **0.800** | **15,443,245.20** | **—（最优）** |
| 0.820 | 15,445,766.23 | +2,521 元 |
| **0.836** | **15,450,067.25** | **+6,822 元** |
| 0.860 | 15,460,124.07 | +16,879 元 |
| 0.900 | 15,503,328.96 | +60,084 元 |

> **★★ 理论预期的 0.836 位移没有出现。** 0.80 仍是实测最优，0.836 反而贵 6,822 元。
> 原因：$a_d$ 虽与净负荷 corr=0.982，但它在总方差里只占 9.2%，而**均匀平移不改变边际权衡**
> （§1.1）——能移动临界比的只有那 9.2% 里与缺口**同时**发生的部分，量级不足以推动分位。
> 这是一条**负结果**，但它同时是对 §1.1 那条核心机制的又一次独立确认。

**调整层分位 τ'**（`TAU_B` 其余三块固定 0.55）：

| τ' | 总费(元) | 与 0.55 之差 |
|---:|---:|---:|
| 0.40 | 15,467,467.43 | +24,222 元 |
| 0.46 | 15,438,324.81 | −4,920 元 |
| 0.48 | 15,434,955.46 | −8,290 元 |
| 0.49 | 15,434,417.04 | −8,828 元 |
| **0.50** | **15,434,133.07** | **−9,112 元（实测最优）** |
| 0.51 | 15,434,232.23 | −9,013 元 |
| 0.52 | 15,435,230.15 | −8,015 元 |
| 0.55 | 15,443,245.20 | —（**定稿**） |
| 0.57 | 15,454,766.39 | +11,521 元 |
| 0.60 | 15,484,427.38 | +41,182 元 |
| 0.65 | 15,548,904.92 | +105,660 元 |

> **⚠ 一个刻意的选择**：τ' 的实测最优是 **0.50**，比定稿的 0.55 便宜 **9,112 元（0.059%）**，
> 峰形平滑单峰（0.48–0.52 全在 15,434k–15,435k），不是噪声。**但定稿仍取 0.55**，理由是：
> ① 0.55 是问题 3 的定稿值，采用它才使「问题 4 与问题 3 策略逐元素相同」这条头条结论成立
> （§6.1）——而那条结论的价值远超 9,112 元；② 0.059% 的差异小于同一参数在 334 天窗口上
> 调优的过拟合量级；③ **这个幅度本身又是「波动电价几乎不改变模型」的一条证据**：
> 换价格后，调整层的最优分位只移动了 0.05，且只值 0.059%。

### 5.3 价格缩放 ±30%（`diag_p4_pscale.py`，文献 3 规范：5% 步长，13 档）

| 缩放 | result4-3 总费(元) | 相对 | result4-2 总费(元) | 相对 |
|---:|---:|---:|---:|---:|
| 0.70 | 10,810,271.64 | −30.00% | 11,235,298.63 | −30.00% |
| 0.85 | 13,126,758.42 | −15.00% | 13,642,862.68 | −15.00% |
| **1.00** | **15,443,245.20** | **—** | **16,050,426.66** | **—** |
| 1.15 | 17,759,731.98 | +15.00% | 18,457,990.60 | +15.00% |
| 1.30 | 20,076,218.76 | +30.00% | 20,865,554.69 | +30.00% |

**实测结论**：

- **策略（`ĝ`/`g`/充/放/紧急 五个数组）在 13 档下逐元素完全相同。**
- 账单**精确线性**：`max|total(s) − s·total(1.00)| = 0.000000 元`（result4-3，走内存数组）；
  result4-2 为 0.116 元，即 334 天逐日 2 位小数取舍之和（容差 1.67 元）——同为零误差。

即：**价格的整体水平对决策零影响，只对账单有比例影响。** 这是 §1.1 那条代数的直接实测。

### 5.4 WQ（电价加权分位）：一个负结果

设计动机：报童临界比是 $c_u/(c_u+c_o)$，若缺口时 $p$ 更高则 $c_u/c_o$ 更大 ⇒ 同一个 τ
对应的 `ĝ` 应当更高。样本实现就是**按电价加权取分位**（高价样本权重更大）。

实测：`WQ=1` 在 τ=0.80 下 15,455,826 元，扫到最优 τ=0.78 也只有 15,453,379 元，
**两档都输给 `WQ=0` 的 15,443,245 元**。故缺省关闭。

唯一例外是 `G=∞` 下 WQ 反号（−33,270 元），说明该机制与购电上界存在交互。
这条留作**未解问题**，不作为交付配置。

### 5.5 购电上界

主表 `G ≤ 5000 kW`。放开反而**更贵** 412,877 元（表 1）——理由与问题 3 相同：
无上界时日前 LP 过度投入套利，紧急购电从 48.9 万涨到 164.8 万元。

> 方案阶段曾预留一条待验证：「波动电价让套利激励更强，这条结论**有可能翻转**」。
> **实测没有翻转**，方向与问题 3 一致。这条已按计划写进本节。

---

## 6. 验证与自检

### 6.1 ★★ 与 `result3.xlsx` 的逐元素对照（头条结论的验证）

| sheet / 列 | 对照结果 |
|---|---|
| 计划购电量 · 逐槽 144 列 | **`max|差| = 0.00000`，相异槽 0** |
| 计划购电量 · 全天购电量列 | `max|差| = 0.0000` |
| 调整购电量 · 逐槽 144 列 | **`max|差| = 0.00000`，相异槽 0** |
| 调整购电量 · 全天购电量列 | `max|差| = 0.0000` |
| 充放电量 | **`.equals() = True`，相异格 0** |
| 紧急购电量 | **`.equals() = True`，相异格 0** |
| 计划购电量 · **全天购电费列** | `max|差| = 13,320.43` 元（**唯一差异**） |
| 全天购电费合计 | 14,712,825.34 → **15,443,245.19**（×1.0496） |
| 派生量 `Σĝ` / `Σg` / 充 / 放 / 日末 SOC / 年末 SOC | 全部相同 |

⇒ **唯一的差异是账单。** 「波动电价不改变最优策略」这句话由两份独立交付物交叉验证，
不是推理，也不是同一份代码跑两遍。

**为什么问题 4-2 没有同样的结论**：`problem4_2.py` 的场景系数（§2.3）进入了目标函数，
故策略确实改变。两者不矛盾 —— 问题 3 的模型里价格只以**当天的单一乘子**进入，
问题 2 的场景系数则让**价格与缺口的联合分布**直接参与。

### 6.2 常数价格回归（`P4_PRICE_SRC=att1`）

| 回归 | 配置 | 结果 |
|---|---|---|
| `problem4_3` → `result3.xlsx` | `P4_PRICE_SRC=att1` | **14,712,825.32 元** ｜ 计划 13,199,466.87 ｜ 违约 0 ｜ 超额 986,018.34 ｜ 紧急 527,340.11 ⟹ **复现 14,712,825 元** ✓ |
| `problem4_2` → `result2.xlsx` | `+ P4_READING=A P4_GMAX=inf` | γ=1 总费 **15,187,755 元**（与定稿一致）✓；逐槽对照：**全天购电量与全天购电费逐日完全相同**（合计 15,134,192.19 元，分毫不差）✓ |

**逐槽对照中的两类差异（必须如实记录）**：

1. 计划购电量 25 格 ±0.0001 kWh、充放电量 4 格 ±0.0001 kWh —— **写入取 4 位小数的
   边界效应**（值恰在 `x.xxxx5` 附近时浮点表示决定舍入方向）。
2. **第 292 天一处 LP 退化多解**：槽 138 与槽 140 之间对调 48.3786 kWh
   （`+48.3786 / −48.3786`），充放电块上对应 `−6.0441 / +6.0441`。
   两槽同属 23:00–24:00 **同一电价档**，故对调**不改变任何费用**（全天购电费分毫不差）。

⇒ 两类都是等价最优/表示精度问题，**不是模型偏差**。回归判定：**实质通过**。

### 6.3 交付表的四层独立审计（`diag_p4_audit.py`）

| 层 | 内容 | 结果 |
|---|---|---|
| **L1** 内存自洽 | 违约费 ≡ 0（推论 1） | **0.0000 元** ✓ |
| | SOC 重建链 vs `soc_day` | `7.913e-11 kWh` ✓ |
| | SOC 区间 / 越限槽 | [1,200.0, 10,800.0] ／ **0 槽** ✓ |
| | 母线定义 `max|e − max(0,L−g−P−d)|` | `0.000e+00 kW` ✓ |
| | 充电可行性（越界槽 ／ 缺口槽充电） | 0 ／ 0（共 52,560 槽）✓ |
| | 账本重算（内存 + 附件 4 实际价） | 四项与结算**逐分一致** ✓ |
| **L2** 表 ↔ 内存 | 逐槽 `ĝ` / `g`（读回 `np.roll(row,1)` 还原） | `5.000e-05 kWh` ✓ |
| | 「全天购电量」列 vs Σ槽 | `0.0034 kWh` ✓ |
| | 两表「全天购电费」逐日一致 | `0.0000 元` ✓ |
| | 充放电块 ／ SOC 列 | `5.000e-05` ／ `4.993e-05 kWh` ✓ |
| | 紧急表 433 行 / 302 段 | 电量 `−0.0003 kWh` ✓（但见 §6.4） |
| **L3** 独立重算 | 只用附件 1/2/4 + 表内数据 | 计划 13,881,916.21 ／ 违约 0 ／ 超额 1,072,358.91；三项小计 vs 内存差 **−0.3804 元**（4 位小数取舍）✓ |
| **L4** 继承性 | 对 `result3.xlsx` 用同一把尺子 | 残差 −2,647,465 kWh ｜ 实际购电 21,643,426 kWh ｜ **−12.23%**，与 result4-3 **完全相同** ✓ |

### 6.4 ⚠ 三个报告陷阱

1. **「紧急购电量」sheet 存的是合并后的连续时段**，每段只有 `(起止时刻, 段内合计 kWh)` ——
   **逐槽的 `e` 在落盘时就丢了**。所以「从表反算费用」必然对不上紧急项：电量对得上
   （差 −0.0003 kWh），**价权对不上**。这是**编码损失，不是账错**；要核紧急项的价权，
   只能回到 `run()` 返回的内存数组。
2. **⚠ SOC 区间不能用日末序列报告**。日末 SOC 区间是 [2,830, 6,000]，而真实日内轨迹
   打满了 [1,200, 10,800]。只用日末序列会严重低报储能的利用强度。
3. **⚠ 哨兵不可跨 `G_MAX` 比较**（表 1 下方的说明）。

---

## 7. 关键结论与保留

### 7.1 ⚠ 交付表的购电量里有 12.23% 既未供负载也未进电池（必须声明）

全窗口能量平衡（L1）：

```
(L−P)Δt 17,939,189 − g 21,643,427 − 放 4,916,804 + 充 6,069,324 − e 95,749 = −2,647,466 kWh
```

**代数上这条残差只可能非正**：令 $S = P + g + d - L$（富余），则 $e = \max(0,-S)$，故

$$\text{残差} = \sum (c - S)\cdot\mathbb{1}[S \ge 0] \equiv -\sum \max(0,\, S - c) \le 0$$

**即残差全部来自「有富余却没充进电池」的部分 —— 富余被无声丢弃。**
（曾怀疑是「缺口槽里充电凭空造电」，L1 已证伪：缺口槽充电 0 槽、充电越界 0 槽。）

- 丢弃电量 **2,647,466 kWh = 实际购电的 12.23%**，丢弃槽占 72.2%。
- 分窗：0:00–6:00 盲窗 13.4% ／ 6–12h **46.0%** ／ 12–18h 29.4% ／ 18–24h 11.2%。
- 含义：账本上是「买了但丢了」，真实微网里等价于**弃光/弃风式削减**，或干脆少买。
- **这是 `problem3_v3.py` 继承下来的性质，不是问题 4 引入的**（L4 对 `result3.xlsx`
  量出**完全相同**的 −2,647,465 kWh）。问题 4 只换价不改量，所以它原封不动地跟了过来。
- `result4-2.xlsx` 同性质，占 **16.39%**（3,678,992 kWh，逐日 max 0 / min −35,534）。

**论文里必须声明。** 它同时说明 `G_MAX=5000 kW` 这道自加上界在夜间是**紧的**（§7.2）。

### 7.2 ⚠ 一条自加假设：`ĝ ≤ 5000 kW`

题面只给了**储能**的 5000 kW 充/放，**未给联络线容量**。`G_MAX` 是本队自加假设，
已在此声明并做了敏感性（§5.5）。贴上限槽占比：result4-3 **36.40%**、result4-2 **36.61%**。

### 7.3 ⚠ 不建模电池退化成本（本项目一贯取舍）

本模型只计购电费用，**不含电池循环退化成本**。因此文献 3 那条
「EFC（等效满循环次数）上升导致盈利下降」的渠道在本模型中**结构性地不存在**，
见 §7.4。

### 7.4 对复核报告 §5 第 10 条（文献 3）的正面回应

> 文献 3 的摘要结论：动态电价最高可把**收益**提高 **28%**，但同时使**成本**提高 **94%**，
> 因 EFC 上升导致**盈利反而下降**。

**本模型的结论与之不矛盾，但落点不同，差异有三条明确来源**：

1. **没有收益侧**。题面只给购电价、输出表只有购电量，微网是**纯购电方**，不向电网售电。
   故文献 3 的「收益 +28%」在本模型中无对应量；能比的只有成本。
2. **成本增幅远小于 94%**。本模型在「纯购电费用」口径下，波动电价的效应是
   **+4.96%**（14,712,825.34 → 15,443,245.19），且**策略完全不变**。
   文献 3 的 +94% 含电池退化等在其口径下计入的成本项。
3. **EFC 渠道未内化**。本模型不计退化成本，故 EFC 上升不会反噬 —— 这是**本模型的局限**，
   已按本项目一贯取舍在此明确声明（§7.3）。若引入退化成本，本模型的成本增幅会**高于** 4.96%。

**敏感性规范已照搬**：文献 3 的 **±30%、5% 步长**已完整执行（§5.3，13 档）。

---

## 8. 结果文件说明

| 文件 | 内容 |
|---|---|
| `结果/result4-3.xlsx` | 4 sheet，334 行 × 147 列。计划购电量 / 调整购电量 / 充放电量 / 紧急购电量 |
| `结果/result4-2.xlsx` | 3 sheet，同上结构（问题 2 无「调整购电量」） |

**写盘约定**：第 k 列装 `arr[(k+1) % 144]`（**时间标签左移**，由附件 5 模板的 10 分钟错位决定）；
读回需 `np.roll(row, 1)` 还原。**已实测确认**：`roll(1)` 还原后「计划 `p·ĝ`」与「超额」
与 `run()` 的结算**逐分一致**，而未平移则差 +188,253.93 元 —— 错误方向是确定的。
写盘末尾有 `assert abs(tot - r["total"]) < 1.0` 自校验。

---

## 9. 复现方法

```bash
# 主交付（读法 B、WQ=0、G≤5000）—— 两个脚本都跑
python 代码/problem4_3.py          # → 结果/result4-3.xlsx
python 代码/problem4_2.py          # → 结果/result4-2.xlsx

# 读法 A（下界）
P4_READING=A python 代码/problem4_3.py

# 常数价格回归自检
P4_PRICE_SRC=att1 python 代码/problem4_3.py            # 须复现 14,712,825 元
P4_PRICE_SRC=att1 P4_READING=A P4_GMAX=inf python 代码/problem4_2.py

# 诊断
python 代码/诊断/diag_p4_price.py      # 电价结构 / 可预测性 / 信息价值
python 代码/诊断/diag_p4_audit.py      # 交付表四层审计
python 代码/诊断/diag_p4_readings.py   # 读法 × 上界 × WQ 矩阵
python 代码/诊断/diag_p4_tau.py        # 分位重扫
python 代码/诊断/diag_p4_pscale.py     # 价格 ±30% 敏感性
```

---

## 10. 文件结构

```
代码/  problem4_2.py(★问题4-2 求解器)  problem4_3.py(★问题4-3 求解器)
       诊断/ diag_p4_price.py   电价结构/可预测性/信息价值
             diag_p4_audit.py   交付表四层审计
             diag_p4_readings.py 口径矩阵
             diag_p4_tau.py      分位重扫
             diag_p4_pscale.py   价格 ±30% 敏感性
             _p4_*.txt          上列脚本的输出存档
结果/  result4-2.xlsx  result4-3.xlsx
文档/  问题4_求解归档.md（本文）
```

> **2026-09-11 清理**：删除了上一轮的探索性残留 `结果/_p4_2_preview.xlsx`（380 KB）
> 与 `代码/诊断/_p4_2_preview.py` —— 后者是 `problem2.py` 的早期变体（把价格换成附件 4
> 直接算），已被 `problem4_2.py` 完全取代。

> **⚠ 已知未修问题（不在问题 4 范围内）**：`代码/make_tables.py:99-104` 的 `tables_p1()`
> 在 `结果/result1.xlsx` 上会抛 `KeyError`。属问题 1 的表格生成遗留，本次未触碰。
