# 问题 2 求解与归档文档

> 题目：2026 年高教社杯全国大学生数学建模竞赛 C 题「微网与外部电网电力调控策略」——问题 2
> 求解日期：2026-09-11
> 求解环境：Python 3 + SciPy（`scipy.optimize.linprog`，HiGHS，稀疏矩阵）
> 求解脚本：`代码/problem2.py`（**唯一的求解器**，生成 `结果/result2.xlsx`）

---

## 0. 结论速览

| 项目 | 数值 |
| --- | ---: |
| **规划层 γ=1 闭式计划总费** | **14,403,793 元** |
| **实际因果执行后总购电费（最终交付，`result2.xlsx` 汇总）** | **13,846,553.27 元** ✓ 独立重读 |
| 　　其中 计划购电费 `Σp·ĝ`（表内 `计划购电量` 列按左移约定重算） | 13,226,936.32 元 |
| 　　其中 实际紧急购电费（= 表内费用列 − 上式） | 619,616.95 元（105,864 kWh，均价 5.853 元/kWh） |
| 完美预见下界（哨兵，任何结果低于它都是 bug） | 12,229,461 元 ⚠ 见 §9 |
| 　⚠ **共同口径**下界（加 `ĝ ≤ 5000 kW`） | **12,406,053 元** |
| 最终方案相对下界 | +13.2%（对 12,229,461）／ +11.6%（对 12,406,053） |
| 实际执行 SOC 范围 | **[1200.0, 10800.0] kWh** ✓（首 6,000.0 ／ 末 6,219.3） |
| ⚠ 购电功率峰值 | **10,325.3 kW** —— **7,313 / 48,096 个槽超过 5000 kW，334 天无一天幸免**（实测，见 §9 第 6 条） |

> **情景集**（本次更新 ★）：旧口径用「同星期几的历史日」(K=4，回看 4 周)，
> 本版改为**按低需求日分组**——先按日均净负荷把七天分成【低需求日】与【其余】，
> 再取回看 **6 天**内与当日同组的历史日。分组规则只喂**暖机期（前 31 天）**，无前视。
> 实测低需求日 = **{周五,周六}**，其日均净负荷只有其余五天的 0.48 倍
> （Fri 254,734 / Sat 256,970 vs 其余 526,753–537,426 kWh/日）。
> 同一因果执行器下：**15,134,192 → 13,846,553 元（−1,287,639，−8.51%）**。
> 归因与显著性见 §7；**窗口为什么取 6 见 §7.4**（这不是随手取的数）。

> **✅ 旧「分项口径待核」警报已解除（假警报）**：旧版把分项写成「计划 13,789,170 + 紧急
> 1,345,023」，同时又说从 `result2.xlsx` 的 `计划购电量` 列重算 `Σp·ĝ` 得到 14,075,113，
> 二者差 285,943，遂怀疑分项有误。**现在查清了：13,789,170 是对的，14,075,113 是错读。**
> 原因是没有应用**循环左移 10 分钟**的时间对齐约定（§10）：取表内 144 个槽列 `sl`，
> 必须用 `Σ_j price_day[j] · sl[:, (j−1) % 144]`（等价于 `np.roll(sl, 1, axis=1)`）
> 才能还原 `Σp·ĝ`；不左移是错读。
> 本版（分组 span6）同一检验：左移还原 **13,226,936.32** vs 求解器 13,226,936 **精确吻合**；
> 不左移得 13,506,848.85。左移还原的紧急费 **619,616.95** vs 求解器 619,617 亦吻合。
> **§6 各表已按左移约定核对，分项可放心引用。**
> 证据脚本：`代码/诊断/_pr1/p2_verify_result2.py`、`代码/诊断/_p2_fresh_stats.py`。

> **账本**：`result2.xlsx` 的费用列 = `Σp·ĝ + 5·Σp·e`，与问题 3 的口径 A **同族**
> （问题 2 无调整通道，故没有 0.5p / 1.5p 两项）。这是两问可对照的基础。

**一句话**：0:00 承诺的只有**购电量 ĝ**；储能充放电是实时追索量，不是一阶段承诺。
`result2.xlsx` 最终记录的是：γ=1 规划得到 ĝ，再用严格 SOC 的因果执行策略在实际
2025 轨迹上执行后的充放电、紧急购电、费用和 SOC。

### 0.1 为什么"闭式计划费"与"实际执行费"是两个数（别混用）

γ=1 的"可交付"只保证 **SOC 递推可行**，**不保证 `d̂` 会被执行**：

- **闭式计划费 14,403,793**：`Σp·ĝ + 5·Σp·max(0, L−P−ĝ−d̂)`，**假设 `d̂` 全部兑现**。
- **实际执行费 13,846,553**：执行层 `execute_soc_causal` **根本不读 `d̂`**，
  按实际缺口重算放电，故紧急购电只有 105,864 kWh（闭式口径是 284,243 kWh）。

两者差 557,240 元，且**实际执行反而更便宜**——因为 `d̂` 里有一部分放电发生在
缺口为零时（本版 544,683 kWh = 8.9%、同星期几口径 809,114 kWh = 13.0%；
口径：逐槽 `max(0, d̂ − max(0, L−P−ĝ))`），照 `d̂` 执行会白付一趟 η 往返损失。
> 若把 `d̂` 连同 SOC 充电限幅一起严格执行，是 **14,442,912 元**（比交付贵 596,359）。
> **论文中报成本请用"实际执行费"。** 详见 §7.6。

---

## 1. 先把题面口径钉死（三条，全部来自 `C题.md` 原文）

1. **「除紧急购电费用外，其他时间段的购电费用均按计划购电量计算」**
   ⟹ 按 **ĝ** 付费，用不掉也算钱。超购的代价就是 `p·Δt`，**没有退款**。
2. **「每天的电价相同」**
   ⟹ 全年共用同一条 144 槽电价剖面，**跨日搬移能量没有价差可套**（见 §3）。
3. **问题 2 没有终端约束。** 题面只给了 `SOC ∈ [1200, 10800]` 与 2025-01-01 0:00 = 6000 kWh。
   「年末回到 6000」是**本项目自加的建模假设**（不加的话最优解会把电池放到 1200 收尾、
   白赚一池电）。**必须在论文中声明这是假设，并做敏感性分析。**

统一账本分两层，避免把规划值和实际执行值混用：

```
规划层（γ=1 LP）
  计划购电费 = Σ p_t · ĝ_t · Δt
  约束目标含情景紧急费，SOC 递推使用情景下界充电量

实际执行层（写 result2.xlsx）
  当前缺口 = max(0, L_t − P_t − ĝ_t)
  d_t = min(缺口, P_MAX, (SOC_t − SOC_MIN)·η/Δt)
  富余 = max(0, P_t + ĝ_t + d_t − L_t)
  c_t = min(富余, P_MAX, (SOC_MAX − SOC_t)/(η·Δt))
  e_t = max(0, L_t − P_t − ĝ_t − d_t)

哨兵：完美预见下界 12,229,461 元（每次报数都必须同时给出）
```

---

## 2. 决策变量划界 —— 本项目旧方案的病根

| 量 | 何时决定 | 依据 |
| --- | --- | --- |
| **ĝ_t 计划购电量** | 每天 0:00，**一阶段承诺** | 题面「请在每天 0:00 制定微网当天的计划购电策略」 |
| c_t, d_t 充放电 | **实时**，二阶段追索 | 由当天实际负荷/光伏决定 |
| e_t 紧急购电 | **实时**，二阶段追索 | 由实际缺口决定 |

旧方案把 **d̂ 也当成 0:00 的承诺量**，并且与**情景平均充电 `E[c̄]`** 写进同一个 SOC 等式。
计划层和执行层于是用了两套储能物理：真实充电在缺电情景被削到 0，
**实际 SOC 恒低于计划 SOC** —— 计划里承诺放出的电在物理上根本不存在。
实测 **914,735 kWh 放电被 SOC 下限卡掉**，报出的总费低于「同一 ĝ 下储能完美预见」的下界，
即**任何策略（含完美预见）都做不到那个数**。

同理，旧口径把紧急购电写成 `e = max(0, L + ĉ − ĝ − P − d̂)`，
把**计划充电当成必须执行**，与题面「供电低于负载才需紧急购电」的措辞冲突。
本题面口径下问题 2 相差 122.5 万元（7.5%）。

---

## 3. 结构：它其实是「报童 + 日内套利」

三条能从题面与数据直接推出的结构，决定了本模型的形态。

### (1) 按日分解 —— 跨日耦合几乎不值钱

各日电价剖面完全相同 ⟹ 跨日搬移能量没有价差可套。实测（确定性点预测模型）：

| 建模方式 | 费用（元） |
| --- | ---: |
| 全年联合 LP（`S₀ = S_T = 6000`） | 13,725,063 |
| 365 个互相独立的单日 LP（每日 `S₀ = S_N = 6000`） | 13,739,828 |
| **差额 = 跨日调度自由度的全部价值** | **14,766（0.108%）** |

### (2) 报童临界比 = 0.8

少买 1 kWh，要在缺口时刻按 `5p` 补，比按 `p` 买**多付 4p** ⟹ 边际缺货成本 `c_u = 4p`；
多买 1 kWh，付 `p` 且用不掉 ⟹ 边际超量成本 `c_o = p`。
临界比 `= c_u / (c_u + c_o) = 4p / (4p + p) = 0.8`。
⟹ 无储能时最优 `ĝ_t` = 净负荷 `(L−P)` 的 **80% 分位**。
（**注意**：常见错误是写成 `5p/(5p+p) = 5/6 ≈ 83%`，那是把总量当边际量。）

**但有储能时这个数字会移动**：两侧代价都被储能削平了 ——
多买可存进电池（净代价 `p − 0.9λ`），少买可由电池放电顶（净代价 `5p −` 放掉那份 SOC 的价值）。
所以 **τ 必须扫，不能从 0.8 直接推**。

### (3) 全部 365 天都是净缺口日

全年缺口 **23.3 M kWh**，富余仅 **3.05 M kWh**（无储能买缺口要 18,254,863 元）。
所以：**弃光几乎不发生**；储能不做「存富余」，只做**日内低买高卖 + 削掉紧急购电的尾巴**。

> **一个必须避开的陷阱**：旧文档里「报童最优分位 ≈83%」与「费用随分位单调降到 q→1.0」
> 看似矛盾，其实同源 —— **K=4 个同伴情景下，0.8 分位就是最大值**（0.8 > 0.75）。
> K=4 根本无法分辨「买到 80 分位」与「买到最大值」。要定位真正的最优分位必须扩大情景数。
>
> ⚠ **换成同组情景集后这个分辨率问题只缓解了一点、没有解决**：本版回看 6 天内，
> 低需求日组平均只有 **3.12** 个同伴、其余五天组约 **5.94** 个（span14 时是 8.11）；
> `q_{γ=1}` 仍是「组内最小值」。**这是 span6 的已知代价**——窗口越短，情景集越小、
> 分位数分辨率越差；它之所以仍值得，是因为分组带来的收益（§7.3 读数 2）远大于此。
> 实测报童面板的最优 τ **在每个配置都卡在扫描网格的边缘 0.7**（应向下延伸网格），
> 且 A2/A3 的排序在 τ=0.7 与 τ=0.8 之间翻转 —— 所以报童面板在本项目里是**弱判别器**，
> §7 的结论一律由 LP 面板与样本外检验承载，不要用报童面板支撑分组主张。

---

## 4. 交付模型：可交付储能计划（deliverable schedule）

把 SOC 递推里的 `E[c̄]` 换成**对覆盖率 γ 的可得充电下确界**：

$$
c_t^{\min} \;\le\; ĝ_t + d̂_t + q_{γ,t},
\qquad q_{γ,t} = \{\,P_{ω,t} - L_{ω,t}\,\} \text{ 的 } (1-\gamma) \text{ 分位}
$$

- **γ = 1** ⟹ `q_t = min_ω (P_{ω,t} − L_{ω,t})`，逐情景全部可交付，**计划层 = 执行层，无模拟歧义**。
- 目标函数仍是 `E[e]` 的**精确值** —— `e` 只依赖 `(ĝ, d̂)`，**与 SOC 无关** ——
  所以给定 `(ĝ, d̂)` 的总费是**闭式**的，**不需要任何执行层模拟**。

> **两个必须避开的建模错误**（本项目都实际踩过）：
> 1. `q` 必须用**裸**`(P−L)`，不能写成 `max(0, P−L)`。后者在缺电时段给出 `q = 0`，
>    于是 `c^min ≤ ĝ+d` 与 `e ≥ L−P−ĝ−d` 同时成立 ——
>    **同一份 `ĝ+d` 既顶负载缺口又给电池充电，凭空造电**（实测全年总费掉到 3,061,112 元，
>    远低于完美预见下界 12,229,461 元）。
> 2. 充电功率上界必须是 `(0, P_MAX)` 而不是 `(0, None)` —— 后者放宽了充电功率限制，
>    会造出一个虚假的低值 14,995,629 元。

### 4.1 规划层 γ 敏感性（统计窗口 2025.2.1–12.31，共 334 天）

| γ | 计划费（元） | 紧急费（元） | 紧急 kWh | 总费（元） | 情景内可交付 |
| ---: | ---: | ---: | ---: | ---: | :--: |
| 0.00 | 11,493,840 | 1,197,749 | 291,966 | 12,691,589 | ✗ |
| 0.25 | 11,918,756 | 1,192,159 | 289,605 | 13,110,915 | ✗ |
| 0.50 | 12,319,788 | 1,187,582 | 287,787 | 13,507,370 | ✗ |
| 0.75 | 12,735,009 | 1,182,818 | 286,141 | 13,917,827 | ✗ |
| **1.00** | **13,226,936** | **1,176,857** | **284,243** | **14,403,793** | **✓** |

γ 表给出的是 LP 规划口径；最终交付另外执行一层严格 SOC 调度，结果见 §4.2。
**只有 γ=1 那一行可作为规划输入**；γ<1 不可交付，不能写进论文。

> **⚠ γ<1 的行原则上允许低于哨兵，不是 bug**（γ<1 的计划不可交付，其闭式价假设
> `d̂` 全部兑现，本就允许低于下界）。**但本版 γ=0 的 12,691,589 已经在哨兵之上了** ——
> 换基线（分组 span6）后情景集更贴合实际，闭式价与可交付口径的差距大幅收窄，
> 于是整张 γ 表**不再有低于哨兵的行**（旧基线 γ=0 为 11,493,229，比下界低 736,232）。
> ⚠ **旧版脚本的哨兵断言写错了**：它拿 `min over 全部 γ` 去比下界，
> 而旧版 γ=0 恰好是 12,534,631（> 12,229,461），**把这个错误掩盖了**。
> 本版已把断言改为只查**交付口径**（γ=1 闭式计划费 与 γ=1 因果执行总费）。

### 4.2 交付方案汇总

| 指标 | 数值 | 单位 |
| --- | ---: | --- |
| 计划购电量 `Σĝ` | 21,734,670.95 | kWh |
| 计划购电费 | 13,226,936.32 | 元 |
| 实际紧急购电量 | 105,863.67 | kWh |
| 实际紧急购电费 | 619,616.95 | 元 |
| **实际执行总购电费** | **13,846,553.27** | 元 |
| 实际充电总量 / 放电总量 | 6,396,664 / 5,183,584 | kWh |
| 实际执行 SOC 区间 | [1200.0000, 10800.0000] | kWh |
| 全年执行 SOC 首 / 末 | 6000.0000 / 6219.3200 | kWh |
| 报告窗口 0:00 / 24:00 SOC | 8759.8798 / 6219.3200 | kWh |
| 全窗口无紧急购电的天数 | 138 / 334 | 天 |
| 有紧急购电的连续时间段 | 311 | 段（分布在 196 天） |

### 4.3 为什么不能拿点预测方案的价格来比

点预测方案（负荷同星期几均值 + 光伏近 7 天均值）也套用同一套严格 SOC 执行策略后：

| 方案 | 计划费 | 紧急费 | 总费 | 实际 SOC 范围 |
| --- | ---: | ---: | ---: | ---: |
| 点预测（同执行层） | 12,202,243 | 3,925,304 | 16,127,547 | [1200.0, 10800.0] |
| **γ=1 + 实际执行** | **13,226,936** | **619,617** | **13,846,553** | **[1200.0, 10800.0]** |

点预测方案计划买得更少，计划费低 102.5 万元；但实际执行时紧急购电多花
330.6 万元（392.5 万 vs 62.0 万），最终比 γ=1 方案贵 **2,280,994 元**
（−102.5 万 + 330.6 万 = +228.1 万）。
这说明 γ=1 的额外计划购电换来了更低的实际紧急购电。

> 点预测那一行**不随情景集改动**：它的 `L_hat`/`P_hat` 是硬编码的旧口径
> （负荷同星期几均值 + 光伏近 7 天均值），与交付情景集无关，故三列数值与换基线前相同。

> **⚠ 注意这张表不能用来论证"情景集该选哪个"。** 这里点预测是"负荷同星期几均值 +
> 光伏近 7 天均值"（沿用旧口径），与 §7 的分组情景集不是同一层次的对象。
> 分组情景集的对照见 §7.1。

---

## 5. 未竟事项：把一阶段决策放进仿真回路标定（**已尝试，失败**）

按「一阶段决策必须对着**可执行的**策略优化」这一原则，做了两条路线的实证：

| 路线 | 做法 | 全年可执行成本（元） |
| --- | --- | ---: |
| A. 日前两阶段 SP 给 ĝ + 因果滚动 MPC 执行 | 储能作追索，日内 SOC 中性 | 19,958,082 |
| B. `ĝ = Q_τ(D)` 报童不动点 + 因果滚动 MPC | τ 扫 0.6–0.9，λ 扫 0–4 | 20,197,808（τ=0.8 最好档） |
| C. 直接用 γ=1 的 ĝ + 因果滚动 MPC | 只检验执行器本身 | 19,443,989（MPC 步长 6 槽）–19,678,011（48 槽） |
| **交付方案 γ=1 + 严格 SOC 因果执行** | **当前缺口优先，富余充电，缺口紧急购电** | **15,134,192**（旧情景集）／**13,846,553**（分组情景集 span6，本版交付） |

**结论：简单、透明且逐槽严格满足 SOC 的因果执行策略优于上述复杂 MPC。**
复杂 MPC 的历史最好结果是 19,443,989 元，但本交付不依赖其跨时段值函数近似。

> A/B/C 三行是**在旧情景集（同星期几 K=4）下**测出的，用来比较**执行器**而不是情景集；
> 换用分组情景集后交付执行费降到 13,846,553，而 MPC 那三行未重测，
> 所以 19,443,989 与 13,846,553 的差**同时含情景集与执行器两个因素**，不可单独归因。
> 若只比同一情景集内的执行器（都是 K=4）：MPC 最好 19,443,989 vs 因果执行 15,134,192，
> 因果执行仍便宜 **4,309,797 元（−22.2%）**。

**复杂 MPC 的根因已定位**：瓶颈不在 `ĝ` 规则，在**执行器的跨时段 SOC 管理**。
标量末端影子价格 λ（元/kWh）呈 **bang-bang** 行为 ——
需要**一个** λ 才会在低缺口日为明天留电，需要**另一个** λ 才会在高缺口日把电放掉，
单一标量无法同时表达这两件事。正确的修法是严格的跨时段/非预期性模型
（144 槽 × K 情景的情景树 + 非预期性约束），**本项目未构建**。

> 这也排除了「执行粒度不够细」这个解释：MPC 步长从 48 槽加密到 6 槽，
> 成本只从 19,678,011 动到 19,443,989（−1.2%），说明瓶颈是**结构性**的，不是分辨率的。

### 5.1 过程中踩过并修掉的坑（记录以免重蹈）

**自指的「日中性」SOC 钉回**：执行器把时域末端 SOC 钉回**当日起点**。
这个约束是**自指**的 —— 它让每一天围绕「当天起点」做能量中性，而当天起点本身
已经被前一天拉低了。于是 SOC 只能单向**棘轮下滑**，掉到下限 1200 后再也回不来。
实测：第 2 天到底（充 2,898 / 放 2,347，**净 SOC 变化恰好 0** ——
电池在下限上原地空转），之后 363 天电池全程瘫痪，所有缺口靠紧急购电，
机械执行只需 18.19 M 的策略被这个缺陷推到 **32.8 M**。

> 这与 §2 的病**是同一类错，只高了一层**：都是让决策对着一个交付不了的反应做优化。

**修法**：把硬钉替换成终端价值 λ 进目标函数。实测 λ 从 0 提到 1，电池立刻恢复循环
（SOC 1200↔10746），高缺口日紧急购电 3,826 → 3,516 kWh。

---

## 6. 结果表（数据源 `结果/result2.xlsx`，与本文档同源）

### 6.1 表 1　指定日期购电量及全天购电量与购电费

| 日期 | 10:00-10:10 | 12:00-12:10 | 14:00-14:10 | 16:00-16:10 | 18:00-18:10 | 20:00-20:10 | 全天计划购电量(kWh) | 全天购电费(元) |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 2025.3.20 | 0.00 | 570.82 | 0.00 | 523.63 | 706.21 | 0.00 | 66,263.06 | 40,953.76 |
| 2025.6.21 | 0.00 | 0.00 | 0.00 | 63.15 | 0.00 | 0.00 | 39,263.40 | 23,120.51 |
| 2025.9.23 | 0.00 | 416.30 | 0.00 | 525.33 | 784.32 | 0.00 | 68,983.78 | 45,319.29 |
| 2025.12.21 | 0.00 | 982.44 | 0.00 | 830.30 | 726.03 | 0.00 | 97,285.30 | 62,138.95 |

> 购电量单位 kWh；「全天购电费」含紧急购电费（= 计划费 + 紧急费）。
> ⚠ 表内「10:00-10:10」等列名是模板的表头；因 §10 的循环左移约定，
> 第 k 列实际存的是 `ĝ[(k+1) % 144]`，即该时刻**后**一个 10 分钟槽的值。

### 6.2 表 2　储能设备在指定日期的充放电量及 0:00/24:00 储电量

**2025.3.20**

| 时间段 | 充电量(kWh) | 放电量(kWh) |
| --- | ---: | ---: |
| 0:00-4:00 | 1834.85 | 55.99 |
| 4:00-8:00 | 580.41 | 4919.34 |
| 8:00-12:00 | 6043.84 | 2777.38 |
| 12:00-16:00 | 3192.33 | 311.07 |
| 16:00-20:00 | 167.06 | 5727.93 |
| 20:00-24:00 | 4476.73 | 3000.76 |
| 0:00 / 24:00 储电量 | **9210.85** / **5218.26** | |

**2025.6.21**

| 时间段 | 充电量(kWh) | 放电量(kWh) |
| --- | ---: | ---: |
| 0:00-4:00 | 125.67 | 597.48 |
| 4:00-8:00 | 1169.49 | 1453.83 |
| 8:00-12:00 | 10322.21 | 0.00 |
| 12:00-16:00 | 0.00 | 0.00 |
| 16:00-20:00 | 50.49 | 6258.41 |
| 20:00-24:00 | 8394.86 | 2545.91 |
| 0:00 / 24:00 储电量 | **2623.60** / **8618.24** | |

**2025.9.23**

| 时间段 | 充电量(kWh) | 放电量(kWh) |
| --- | ---: | ---: |
| 0:00-4:00 | 1892.73 | 26.80 |
| 4:00-8:00 | 33.80 | 6337.77 |
| 8:00-12:00 | 5455.89 | 1896.62 |
| 12:00-16:00 | 4814.38 | 626.83 |
| 16:00-20:00 | 175.99 | 5731.05 |
| 20:00-24:00 | 8595.86 | 2554.62 |
| 0:00 / 24:00 储电量 | **9118.44** / **8908.35** | |

**2025.12.21**

| 时间段 | 充电量(kWh) | 放电量(kWh) |
| --- | ---: | ---: |
| 0:00-4:00 | 2358.62 | 57.80 |
| 4:00-8:00 | 1255.63 | 1477.47 |
| 8:00-12:00 | 5788.70 | 7278.92 |
| 12:00-16:00 | 7022.45 | 2283.77 |
| 16:00-20:00 | 19.05 | 5728.22 |
| 20:00-24:00 | 8444.40 | 2842.53 |
| 0:00 / 24:00 储电量 | **8348.21** / **8894.05** | |

### 6.3 表 3　微网在指定日期的紧急购电量

| 日期 | 时间段 | 紧急购电量(kWh) | 当日合计(kWh) |
| --- | --- | ---: | ---: |
| 2025.3.20 | — | 0.00 | 0.00 |
| 2025.6.21 | 06:20-07:20 ／ 20:30-20:40 | 376.29 ／ 63.41 | 439.71 |
| 2025.9.23 | 20:20-20:40 ／ 20:50-21:00 | 337.89 ／ 29.17 | 367.06 |
| 2025.12.21 | — | 0.00 | 0.00 |

> γ=1 的机制是用计划多买换来实际紧急购电的下降：在同一严格 SOC 执行层下，
> 点预测方案全年紧急购电 660,419 kWh、总费 16,127,547 元；
> 分组 γ=1 方案（span6）紧急购电 105,864 kWh、总费 13,846,553 元。
>
> ⚠ **注意 span6 与 span14 两种分组的取舍方向不同，别拿采样日以偏概全**：
> 旧 K=4 口径这四个采样日是「0 / 0 / 438.30 / 111.86 kWh」，
> span14 分组是「0 / 0 / 6.38 / 0」，而**本版 span6 是「0 / 439.71 / 367.06 / 0」**。
> span6 用**更高的全年紧急量**（105,864 kWh，对比 span14 的 42,589 kWh）
> 换来了**更低的计划费**（13,226,936 对比 14,089,579），总账仍最省
> （13,846,553 对比 14,341,723）。窗口扫描与边界逻辑见 §7.4。

---

## 7. 情景集：分组收益与归因

本版把情景集从「同星期几的历史日」换成「按低需求日分组」，收益是 **−1,287,639 元（−8.51%）**。
但这一节要回答的是：**这笔收益该记给谁、回看窗口该取多长？** 结论对论文措辞有直接影响。

### 7.1 分组的数据基础

分组规则：按**日均净负荷**把七天分成【低需求日】与【其余】两组，取回看 **6 天**内同组的历史日。
规则只喂**暖机期（前 31 天，在报告窗口之前）**，因此推导过程**无前视**。
（为什么窗口是 6 而不是 7 或 14，见 §7.4——**这不是随手取的数**。）

| 星期 | 三 | 四 | 五 | 六 | 日 | 一 | 二 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 日均净负荷（全年, kWh/日） | 415,597 | 409,812 | **134,885** | **137,321** | 410,314 | 410,071 | 413,178 |

周五、周六只有其余五天的 **1/3**（暖机期内是 1/2）。**周日完全是工作日行为**——
这一点曾让本项目误判过一次（按日历直觉以为该分周末，见 §7.2 的 A4 行）。

分组规则对推导窗口**不敏感**，四个窗口都给出同一答案：

| 推导窗口 | 推出的低需求日 |
| --- | --- |
| 暖机期 d0–30（31 天，**本版采用**） | {周五, 周六} |
| 1–6 月（182 天） | {周五, 周六} |
| 全年（365 天） | {周五, 周六} |
| 报告窗口 d31–364 | {周五, 周六} |

### 7.2 情景集对照（统一 γ=1 规划 + `execute_soc_causal`，334 个报告日）

| 情景集 | 平均情景数 | 因果执行总费（元） | 相对 A1 | 判定 |
| --- | ---: | ---: | ---: | --- |
| **A1 同星期几 K=4（旧基线）** | 3.81 | **15,134,192** | — | 基线 |
| A2 同星期几 K=2 | 1.94 | 15,041,540 | −92,652 | 不显著 |
| **A3 低需求日 {周五,周六} span6（本版交付）** | 3.12 | **13,846,553** | **−1,287,639** | **显著** |
| 　分组 span7 | 4.10 | 13,870,178 | −1,264,014 | 显著 |
| 　分组 span14（旧交付） | 8.11 | 14,341,723 | −792,470 | 显著 |
| A4 日历周末 {周六,周日} span6 | 3.12 | 22,104,625 | +6,970,433 | 显著更差 |
| 　日历周末 {周六,周日} span14 | 8.11 | 16,887,089 | +1,752,897 | 显著更差 |
| A5 不分组（全七天 span6） | 5.94 | 16,403,378 | +1,269,186 | 显著更差 |
| 　不分组（全七天 span14） | 13.71 | 17,099,172 | +1,964,980 | 显著更差 |

> **篮子可信度**：A1（15,134,192）、A4-span14（16,887,089）、A5-span14（17,099,172）
> 三个**换分组之前**的旧臂全部被本篮子精确复现 ✓，A3-span14 复现 14,341,723 ✓。
> 扫描表见 §7.4（脚本 `代码/诊断/_p2_span_scan.py`、`_p2_span6_attr.py`）。

### 7.3 归因：收益来自「分组」还是「窗口更近」

A3 相对 A1 同时改了两件事（分组 **和** 回看窗口从 4 周缩到 6 天），所以必须拆开。
14 天块自助、逐日总费、4000 次重抽：

| 对比 | Δ总（元） | 95% CI | p | 中位数日 | 更便宜天数 | 剔除最省 10 天 | 剔除 20 天 |
| --- | ---: | --- | ---: | ---: | ---: | ---: | ---: |
| **A3−A1　分组 + 缩短窗口（合计）** | **−1,287,639** | [−5,300, **−2,654**] | **< 0.001** | −2,458 | **260 / 334** | −924,962 | −761,845 |
| A2−A1　**只换窗口**（同星期几 K4→K2） | −92,652 | [−1,452, **+1,014**] | 0.648 | −1,085 | 232 / 334 | −14,757 | **+48,978** |
| 分组 span6 − 全七天 span6　**只换分组**（窗口相同） | −2,556,824 | [−8,192, −7,108] | **< 0.001** | 0 | 162 / 334 | −2,203,443 | −1,877,570 |
| A3−A2　（窗口+分组一起换，仅作参考） | −1,194,987 | [−5,860, −1,769] | **< 0.001** | −670 | 199 / 334 | −712,287 | −462,875 |
| span6 − span14　（换交付口径） | −495,169 | [−2,181, −792] | **< 0.001** | −1,612 | 273 / 334 | −413,605 | −356,340 |
| A4−A1　日历周末 span6 − K=4 | **+6,970,433** | [+18,705, +23,197] | < 0.001 | +792 | 150 / 334 | +7,288,307 | +7,388,418 |

**四条读数：**

1. **「窗口变近」本身不构成收益**（A2−A1 的 CI 跨 0，p=0.648，剔除最省 20 天后**翻正**）。
   所以 A3 的好处不是靠"使用更近的数据"骗来的。
2. **「分组」本身显著，且在窗口匹配的对照下贡献最大**：同一个 6 天窗口内，把"同组日"
   换成"全部历史日"要多花 **2,556,824 元**（CI [7,108, 8,192]，不跨 0），
   剔除最省 20 天后仍有 **+1,877,570**。**这是本版最干净的一条「只换分组」证据。**
   > ⚠ 但量级不可当"分组技能的通用价值"读：`全七天 span6` 把 {周五,周六} 的低负荷
   > 混进了每个工作日的场景集，属于**错误模型**而非**次优模型**（计划费 16.1 M 已跑偏），
   > 所以这个差额里含"纠正一个错误"的成分，比"分组能赚多少"要大。
3. **合计效应稳健，不再是尾部驱动**：A3−A1 中位日 −2,458、260/334 天更便宜，
   剔除最省 10 天剩 −924,962、20 天剩 −761,845——**不翻正**。
   （旧 span14 口径下 A3−A2 剔 10 天只剩 −210,939、剔 20 天翻正，那个"尾部驱动"的
   特征在 span6 上**已消失**；旧口径的结论不可再引用。）
4. **日历周末是灾难**：A4 span6 的紧急购电 **1,405,504 kWh**（A3 是 105,864），总费 +697 万。
   原因是**周日表现得像工作日**——把 {周六,周日} 分成一组，等于给周日配了一个
   低负荷情景集 ⟹ `ĝ` 系统性低估。

**论文措辞建议（已按 span6 更新）：**

- ✅ 可以主张：**「低需求日分组 + 回看 6 天」这个配方优于旧口径的同星期几 K=4**：
  −1,287,639 元（−8.51%），CI [−5,300, −2,654]，p<0.001，中位日与省钱天数占比双双支持，
  且剔除最省的 20 天后仍有 −761,845（不翻正）。
- ✅ **也可以主张「分组」这个动作本身显著**——依据是**窗口匹配**的
  `分组 span6 vs 全七天 span6`（−2,556,824，CI 不跨 0，剔 20 天仍 −1,877,570），
  而**不是**把 A3−A1 的功劳记给分组。（这条比 §7.2 旧版的措辞**更强**，因为换了 span6。）
- ⚠ 仍**不要**主张"分组比同星期几更好"：`A3−A2` 是显著的，但它同时改了窗口（6 天 vs ≈2 周）
  与分组，**不可单独归因**；`A2−A1` 又说明窗口本身不值钱——两条合起来正好说明
  A3−A2 的收益只能记给"分组 + 窗口共同作用"，不能拆。
- ⚠ **不要**引用 A4 的"日历周末"作对比来暗示"我们找到了真正的周末"。
  数据里周六周日**不同组**，`{周六,周日}` span6 比基线**贵 697 万**。

### 7.4 ★ 回看窗口为什么取 6 天（窗口扫描，14 臂）

旧版 `GROUP_SPAN = 14` 是**直接选的、从未验证过**（原 §7.2 的五臂里没有任何窗口变体），
并导致与问题 3 的 `P3_GSPAN = 7` 无声不一致。本次补齐扫描（γ=1 可交付 + `execute_soc_causal`，
334 个计费日；脚本 `代码/诊断/_p2_span_scan.py`）：

| `span` | 分组臂总费（元） | vs span14 | 计费窗口内「前视回退」天数 | 判定 |
| ---: | ---: | ---: | ---: | --- |
| 4 | 13,621,172 | −720,551 | **47** | ⚠ **作废，见下** |
| 5 | 13,622,447 | −719,276 | **47** | ⚠ **作废，见下** |
| **6** | **13,846,553** | **−495,169** | 0 | ✓ **本次采用（= 交付值）** |
| 7 | 13,870,178 | −471,545 | 0 | ✓ |
| 8 | 13,957,388 | −384,335 | 0 | ✓ |
| 10 | 14,067,328 | −274,395 | 0 | ✓ |
| **14（旧定稿）** | **14,341,723** | — | 0 | ✓（旧交付值，精确复现 ✓） |
| 21 | 14,713,463 | +371,740 | 0 | ✓ |
| 28 | 15,032,496 | +690,773 | 0 | ✓ |

**（a）为什么 4/5 作废——它们不是"更短的窗口"，是"没有窗口"**

低需求日 **{周五, 周六} 每周只有 2 天**，所以回看 `s` 天能捞到的同组日只有 **≈2s/7** 个：

- `s = 6`：窗口（上周四…本周二）里周五、周六**各恰好 1 个** ⟹ 情景集非空。
- `s ≤ 5`：窗口（上周六…本周三 或更短）里**一个同组日都没有** ⟹ `build_peers_group` 返回空表，
  `sc_of` 退化成 `[d]` **自身**——**拿当天实际负荷冒充"情景天"，是前视作弊**。
  两个臂的 13.62 M 是这么来的，**不是策略更优，是偷看了答案**。

纯 numpy 核验：span 4/5 在计费窗口内各有 **47 天**踩此回退（平均情景数 1.99 / 2.41），
span 6/7/8/14 均为 **0 天**（3.12 / 4.10 / 4.79 / 8.11）。
⟹ **`s = 6` 是第一个干净窗口，即可行域的下界。**

**（b）在干净可行域（`s ≥ 6`）上，总费严格单调递增** ⟹ 越短越好，**可行最优 = 可行下界 = 6**。
（6 → 7 只差 23,625 元、8 差 110,835，说明这个最优是**平坦**的，不是尖峰；
21/28 明显变差才是真信号。这与 §7.3 读数 2 「窗口匹配下分组贡献最大」并不矛盾：
**分组**的收益很大，但**窗口**在可行域内的边际只值 2 万量级。）

> **★ 诚实边界（论文必须写）**：**6 是"约束下的最优"，不是"无约束最优"。**
> 真实最低点可能落在 `s ≤ 5`，但**那片区域被前视污染、测不了**——测出来的 13.62 M
> 是作弊值，不可引用。所以本版只能说：*在"情景集必须只由历史日构成"这条硬约束下，
> 回看窗口取 6 天最优。* **不要**写成"我们找到了全局最优窗口"。

**（c）对照组：只缩窗口、不分组**（`all7` 臂 = 过去 `s` 天内**全部**历史日）：

| `span` | 分组臂 | 全七天臂（只缩窗口） | 分组增益 |
| ---: | ---: | ---: | ---: |
| 5 | 13,622,447 ⚠ | 16,335,559 | −2,713,112 |
| 6 | 13,846,553 | 16,403,378 | −2,556,824 |
| 7 | 13,870,178 | 16,491,762 | −2,621,584 |
| 10 | 14,067,328 | 16,789,000 | −2,721,672 |
| 14 | 14,341,723 | 17,099,172 | −2,757,449 |
| 21 | 14,713,463 | 17,556,300 | −2,842,837 |

**★ 分组增益（同窗口、只切换组掩码）稳定在 256~284 万元、随窗口拉长只缓慢增加**，
说明这笔收益几乎全部来自「把低需求日从工作日的情景集里剔除」，**与窗口长短基本无关**
——这正是 §7.3 读数 2 的配对证据（span6 那一行已在 §7.3 给出 CI 与剔尾结果；
span7 那一行的 CI 是 [−8,364, −7,338]，剔最省 20 天后仍 −1,933,693）。
⚠ 但**量级不可当"分组技能的通用价值"读**：全七天臂把 {周五,周六} 的低负荷混进了
每个工作日的场景集，属于**错误模型**而非**次优模型**（计划费 16.1 M 已跑偏），
差额里含"纠正一个错误"的成分。

### 7.5 样本外复核（分组规则只用 1–6 月推导，成本只在 7–12 月评估）

| 配置 | 7–12 月窗口成本（元） | 相对基线 |
| --- | ---: | ---: |
| C1 同星期几 K=4（基线） | 8,679,124 | — |
| **C2 低需求日（样本外分组）** | **8,233,834** | **−445,290** |
| C3 日历周末 {周六,周日} | 9,684,088 | +1,004,964 |
| C4 同星期几 K=2 | 8,786,709 | +107,585 |

仅用 1–6 月推出的分组**仍是 {周五,周六}**，在完全独立的 7–12 月窗口上省 445,290 元。
这是本版分组最有力的一条证据：**规则没有过拟合到评估窗口**。

### 7.6 一阶段承诺边界：`d̂` 被"兑现"了吗（PR #1 的症结）

`d̂`（计划放电）在 LP 里是**一阶段变量**——长度 `T`、只按 `t` 索引、不按场景索引，
即同一时刻全场只有一个放电量，全年在 0:00 一次定死。真正的追索量只有充电与紧急购电。

| 口径 | 做什么 | A1（同星期几 K=4） | A3（分组 span6，本版交付） |
| --- | --- | ---: | ---: |
| 闭式总费 | 假设 `d̂` 全部兑现，不模拟 SOC | 15,187,755 | 14,403,793 |
| **严格执行 `d̂`**（`min(d̂, SOC限)`，不减缺口） | `d̂` 放出 → 顶高富余 → 又被充回 | 15,226,839 | **14,442,912** |
| **`execute_soc_causal`（本版交付）** | **丢弃 `d̂`**，按实际缺口重算放电 | **15,134,192** | **13,846,553** |

本版 `d̂` 中「缺口为零时仍放电」的量，**两种口径**（必须写明用的是哪一个）：

| 口径 | 判据 | A1（K=4） | A3（分组 span6） |
| --- | --- | ---: | ---: |
| **实际轨迹口径**（"白付 η 往返"的量） | 逐槽 `max(0, d̂ − max(0, L−P−ĝ))` | 809,114 kWh（13.0%） | **544,683 kWh（8.9%）** |
| 情景最坏口径（"不放电也能覆盖全部情景"） | `ĝ + d̂ + q ≥ L − P`，`q = min_ω(P−L)` | 135,880 kWh（2.2%） | 78,537 kWh（1.3%） |

照 `d̂` 严格执行，这笔电放出去又被充回来，白付一趟 η 往返损失：紧急购电从
**105,864 涨到 290,118 kWh**，**总费 +596,359 元**。
（换基线前 span14 口径下这两个数是 801,803 kWh / 14.2%、+610,923 元，**勿再引用**。）

> **两个必须写进论文的结论：**
> 1. **0:00 承诺的只有 `ĝ`**。题面从没说储能要走一份预先排定的充放电时序；恰恰相反，
>    "实时发现供电不足再紧急购电"就是追索行为的定义。`d̂` 只是 LP 的副产品，不是承诺量。
> 2. **"闭式计划费"与"执行费"是两个数**，SOC 连续性检查通过也**不能**推出
>    "计划层 = 执行层"。γ=1 保证的只是 **SOC 递推可行**，不是 `d̂` 会被执行。
>    **一个反直觉但重要的读数**：本版**因果执行（13,846,553）比严格执行 `d̂`（14,442,912）
>    便宜 596,359 元** —— 也就是说 LP 给的放电计划**照做反而更差**，这本身就证明它不该被
>    当成承诺量。

证据脚本：`代码/诊断/_pr1/p2_commit_check.py`、`p2_exec_decomp.py`；
本节的 span6 重测：`代码/诊断/_p2_dhat_edge.py`（两个口徑都在里面）、`_p2_fresh_stats.py`。

> **面板 B（报童分位）是弱判别器**：ĝ = τ 分位(净需求 L−P) 直接扫 τ 时，
> 最优 τ 在**每个配置都卡在网格边缘 0.7**（应向下延伸网格），且 A2 与 A3 的排序
> 在 τ=0.7 与 τ=0.8 之间**翻转**。所以本节的结论由面板 A（LP）与 §7.5（样本外）承载，
> **不要**用报童面板支撑分组主张。

---

## 8. 验证与自检

| 检查项 | 结果 | 结论 |
| --- | --- | --- |
| 哨兵断言（脚本内 `assert`） | γ=1 闭式计划费 14,403,793 与因果执行费 13,846,553 均 ≥ 12,229,461 | 模型未被放松 |
| LP 状态 | `Optimal`（HiGHS） | 全局最优 |
| **实际执行 SOC 区间** | **[1200.0000, 10800.0000] kWh** | 无越界 ✓ |
| 实际执行 SOC 首 / 末 | 6000.0000 / 6219.3200 kWh | 题面无终端约束 |
| 充放电功率 | 全年块峰值 **2,666.67 / 2,145.58 kW**（每块 4h，10,666.67 / 8,582.31 kWh） | 满足 5000 kW ✓ |
| 同时充放电 | 逐槽互斥（`execute_soc_causal` 单步递推保证） | 物理执行明确 |
| `result2.xlsx` 三表 | 334×147 ／ 2004×6 ／ **449×3** | 与附件 5 模板字段一致 |
| 槽列和 vs 全天购电量 | 21,734,670.8799 vs 21,734,670.9526 | 一致（末位舍入，差 0.073） |
| 全天购电费列合计 | **13,846,553.27 元** | 与实际执行总费对齐 |
| `Σp·ĝ` 左移约定还原 | 13,226,936.32 vs 求解器 13,226,936 | 精确吻合 ✓ |
| SOC 跨日连续性 | 24:00 与次日 0:00 最大差 **0.0000000000** | 链条闭合 ✓ |
| 日期范围 | 2025-02-01 → 2025-12-31 | 334 天，1 月为暖机期 |
| NaN 检查 | 0 | 通过 |

---

## 9. 论文必须写明的保留

1. **γ=1 的规划保证只针对建模情景集（按低需求日分组的历史日，见 §7.1）**，
   不是分布无关的鲁棒保证。最终 `result2.xlsx` 使用实际 2025 轨迹重放严格 SOC 执行策略，
   并单独报告实际结果。
2. **0:00 承诺的只有 `ĝ`，`d̂` 不是承诺量**（见 §7.6）。`d̂` 中 8.9% 的放电发生在缺口为零时，
   执行层不兑现它。**不要**把"闭式计划费"当成交付成本。
3. **年末 SOC = 6000 只在规划层使用**。题面没有终端约束，实际因果执行不强制年末回到 6000；
   本次实际执行终值为 6219.3200 kWh，全程满足 SOC 上下限。
4. **本方案是因果执行策略，不是「无预报在线最优策略」。**
   执行器只使用当前实际 L/P 和当前 SOC，未做跨日最优值函数标定；论文中不要声称在线最优。
   因此 **13,846,553 是上界**，真实最优落在 [12,406,053, 13,846,553] 之间。
5. **完美预见下界 12,229,461 元必须同时给出。交付口径（γ=1）低于它才是 bug**
   （本项目在建模过程中三次造出低于下界的数，全部是模型错误，见 §4 的两个陷阱与 §5.1）。
   ⚠ 但 **γ<1 各行低于下界是正常的**，它们不可交付、闭式价假设 `d̂` 全部兑现，
   不是 bug，也不要拿它们触发哨兵断言（见 §4.1 的警告框）。
6. ⚠ **该下界含一个未声明的假设：购电功率无上界。**
   **题面（附录 1）只给了储能「最大充放电功率 5000 kW」，并没有给微网与外网的联络线容量。**
   `problem2.py` 把购电变量建成 `(0, None)`，其哨兵 LP 解出 **g_max = 10,325.33 kW**
   （物理上限的两倍），所以 **12,229,461 偏低**。把 `G ≤ 5000` 加回去，同一条 LP 给出
   **12,406,052.74 元**——与 `problem3_v3.sentinel()`（问题 3 口径 A）**精确到分相同**，
   两条独立写的 LP 相互印证。证据脚本 `代码/诊断/diag_sentinel_p2p3.py`。

   **后果**：`结果/result2.xlsx` 的交付计划**有 7,313 / 48,096 个槽超过 5000 kW**（峰值即 10,325.3 kW），
   即本方案**只在「购电功率无上限」的字面读法下可行**；在保守假设（联络线 ≈ 储能同量级）下不可交付。
   `结果/result3.xlsx` 是 0 槽越限。**论文必须交代这一点，并说明两条哨兵（12,229,461 与 12,406,053）
   不可并列使用；与问题 3 对照时应统一取共同口径 12,406,053 元。**
   ⚠ **越限槽数随情景集变化（三个臂都已实测，`代码/诊断/_p2_gcap.py`）**：
   K=4 旧基线 **7,801** ／ 分组 span14 **8,192**（最多）／ **本版分组 span6 是 7,313（最少）**；
   三者的 Σĝ 分别是 22.56 M ／ 23.02 M ／ **21.73 M kWh**。
   ⟹ 改用 span6 **同时**把越限槽数降回三个配置里的最低，这个口径问题**略有缓解**，
   但**没有消除**——仍有一半以上（7,313 / 48,096 ≈ 15.2%）的槽越限、334 天无一幸免。

---

## 10. 文件结构与复现

```
数模国赛C题/
├── 代码/
│   └── problem2.py                    # 问题 2 唯一求解器 → 结果/result2.xlsx
├── 结果/
│   └── result2.xlsx                   # 问题 2 交付文件（γ=1 可交付计划）
└── 文档/
    ├── 问题2_求解归档.md               # 本文档
    └── 问题2_独立复核_可交付性与追索模型.md   # 复核证据（为何旧方案不可用）
```

```bash
python 代码/problem2.py                          # 求解 γ=1，覆盖写 结果/result2.xlsx
GAMMAS=0.5,0.75,1.0 python 代码/problem2.py      # γ 敏感性（仅 γ=1 可引用）
```

- `problem2.py` 自动：读附件 1/2 → 用**暖机期（前 31 天）**推导低需求日分组 →
  构造同组情景集（回看 14 天）→ 算 `q_{γ,t}` → 解 γ=1 规划 LP →
  `execute_soc_causal` 在实际 2025 轨迹上严格执行 → 重算充放电、紧急购电、费用与 SOC →
  打印同星期几 vs 同组的对照 → 哨兵与边界断言 → 覆盖写 `结果/result2.xlsx`。
- 诊断脚本（`代码/诊断/_pr1/`，**脚本入库，中间产物 `*.xlsx` 按 `.gitignore` 忽略**）——
  §7 与 §8 的每个数字都由它们产生，可逐条重跑：
  `p2_group_experiment.py`（五情景集对照 + 归因 + 样本外，产出 §7.2–§7.5）、
  `p2_pairwise.py` / `p2_pairwise_probe.py`（A3−A1 / A3−A2 配对块自助、符号检验、
  剔除 N 天稳健性、块长敏感性）、`p2_pairwise_dates.py`（尾部日期聚类定位）、
  `p2_newsvendor.py`（报童面板 = 弱判别器，见 §3(2) 警告框）、
  `p2_commit_check.py`（`d̂` 一阶段承诺实测，产出 §7.6）、
  `p2_verify_result2.py`（只读 `result2.xlsx` 的独立复核，产出 §8 逐行）。
  同目录另存 PR #1 的原始文件 `problem2_stochastic_group.py` 与 `问题2求解报告.md`，
  作为 §4.2 / §7.6 诊断对象的留档。
- **时间对齐约定**（与问题 1 一致）：附件 2 时间列是**右端点**标注（`0:10` 代表 `[0:00, 0:10]`）；
  result2 模板「计划购电量」列标签是**起止点**标注且从 `0:10-0:20` 起步，
  故写入采用**循环左移 10 分钟**：`result2[k] = p[(k+1) % 144]`。
  紧急购电量 sheet 的时间段为**绝对时间**。
