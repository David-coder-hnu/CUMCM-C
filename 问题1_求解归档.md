# 问题 1 求解与归档文档

> 题目：2026 年高教社杯全国大学生数学建模竞赛 C 题「微网与外部电网电力调控策略」——问题 1
> 求解日期：2026-09-10
> 求解环境：Python 3 + SciPy（`scipy.optimize.linprog`，HiGHS 求解器）

---

## 1. 问题概述

若每天的电价和小区负载相同，同时给出光伏发电功率一天的预测数据，要求：

1. 微网提供的电能**不可低于**小区负载；
2. 储能设备在 **0:00 与 24:00 的储电量相同**；
3. 在每天 **0:00 制定当天**的计划购电策略；
4. 目标：**尽可能节省购电费用**。

由于问题 1 无任何随机性（电价、负载、光伏全部已知），"计划购电"即"实际购电"，本质是一个**确定性线性规划（LP）**：储能套利 + 光伏消纳。

---

## 2. 数据与参数

### 2.1 附件 1（问题 1 数据）

- 单日 144 个 10 分钟区间（时间列 `0:10, 0:20, …, 23:50, 0:00+1`，即右端点标注，`0:00+1` = 当天 24:00）。
- 三列：**电价**（元/kWh）、**小区负载**（kW）、**光伏发电预测功率**（kW）。

| 指标 | 数值 |
| --- | --- |
| 电价范围 | 0.3713 ~ 1.3952 元/kWh |
| 负载范围 | 3309 ~ 5959 kW |
| 光伏范围 | 0 ~ 7612 kW（5:40 起有输出，12:10 达峰，19:10 归零） |
| 全天负载总能量 | 111025 kWh |
| 全天光伏总能量 | 55483 kWh |
| 全天净负荷（负载−光伏） | 55542 kWh |

### 2.2 附录 1（储能参数）

| 参数 | 数值 |
| --- | --- |
| 最大容量 | 12000 kWh |
| 最大充放电功率 | 5000 kW |
| 初始电量（2025-1-1 0:00） | 6000 kWh |
| 电量上下限 | 1200 ~ 10800 kWh |
| 充放电效率 | 90%（充电、放电各 9 折，往返 81%） |

### 2.3 建模关键设定（已与团队确认）

- **效率方向**：充电、放电各打 9 折（`SOC += 0.9·c·Δt`，放电时电池吐出 `d·Δt/0.9`），往返效率 81%。
- **不允许向电网售电**：题目只谈"购电"、附件只给"电价"（购电价）而无"售电价"，多余光伏弃光处理。
- **首尾储电量固定 6000 kWh**：`SOC(0) = SOC(24) = 6000`（初始值取自附录 1）。

---

## 3. 数学模型

时间离散为 144 个区间，`Δt = 1/6` 小时（10 分钟）。设 `t = 0, 1, …, 143`。

### 3.1 决策变量（每区间 t）

| 变量 | 含义 | 范围 | 单位 |
| --- | --- | --- | --- |
| `g_t` | 外网购电功率 | ≥ 0 | kW |
| `c_t` | 储能充电功率 | [0, 5000] | kW |
| `d_t` | 储能放电功率 | [0, 5000] | kW |
| `s_t` | 弃光功率 | ≥ 0 | kW |
| `SOC_t` | 区间 t 起始时刻储电量 | [1200, 10800] | kWh |

其中 `L_t`、`P_t` 分别为区间 t 的负载与光伏功率（已知常量）。

### 3.2 目标函数（最小化购电费）

$$
\min \; \sum_{t=0}^{143} \text{price}_t \cdot g_t \cdot \Delta t
$$

### 3.3 约束

**① 功率平衡**（供给 = 需求，多余光伏记入弃光）：

$$
g_t + P_t + d_t = L_t + c_t + s_t
$$

**② 储电递推**（充放电各 9 折）：

$$
SOC_{t+1} = SOC_t + 0.9 \cdot c_t \cdot \Delta t - \frac{d_t \cdot \Delta t}{0.9}
$$

**③ SOC 上下限**：$1200 \le SOC_t \le 10800$

**④ 首尾相等（能量中性）**：$SOC_0 = SOC_{144} = 6000$

**⑤ 功率上限**：$0 \le c_t \le 5000$，$0 \le d_t \le 5000$，$g_t \ge 0$，$s_t \ge 0$

> **说明**：同一区间不会同时充放电（同时 `c_t>0` 且 `d_t>0` 时净 SOC 变化为 `0.9−1/0.9 < 0`，纯亏能量），故无需额外添加 `c_t·d_t=0` 约束。光伏免费，优化器自动优先消纳光伏，仅在"光伏 > 负载 + 可充容量"时弃光。

---

## 4. 求解方法

用 `scipy.optimize.linprog(method="highs")` 直接求解上述线性规划：

- 变量数：`144×4 + 145 = 721`；
- 等式约束：`288` 条（144 条功率平衡 + 144 条 SOC 递推）；
- 求解耗时：毫秒级，返回 `Optimal`。

---

## 5. 求解结果

### 5.1 全天汇总

| 指标 | 数值 | 单位 |
| --- | --- | --- |
| **全天购电量** | **59482.70** | kWh |
| **全天购电费** | **35126.95** | 元 |
| 全天充电量 | 20740.67 | kWh |
| 全天放电量 | 16799.94 | kWh |
| 全天弃光量 | 0.00 | kWh |

### 5.2 表 1　微网在指定时间段的购电量及全天的购电量和购电费

| 时间段 | 购电量(kWh) | 时间段 | 购电量(kWh) | 时间段 | 购电量(kWh) |
| --- | --- | --- | --- | --- | --- |
| 10:00-10:10 | 0.00 | 12:00-12:10 | 480.41 | 14:00-14:10 | 0.00 |
| 16:00-16:10 | 445.43 | 18:00-18:10 | 531.89 | 20:00-20:10 | 0.00 |
| **全天购电量** | **59482.70** | **全天购电费** | **35126.95 元** | | |

### 5.3 表 2　储能设备在指定时间段的充放电量及 0:00 和 24:00 的储电量

| 时间段 | 充电量(kWh) | 放电量(kWh) | 时间段 | 充电量(kWh) | 放电量(kWh) |
| --- | --- | --- | --- | --- | --- |
| 0:00-4:00 | 4500.00 | 0.00 | 4:00-8:00 | 833.33 | 6365.84 |
| 8:00-12:00 | 4787.96 | 1703.00 | 12:00-16:00 | 5286.04 | 91.10 |
| 16:00-20:00 | 0.00 | 5780.13 | 20:00-24:00 | 5333.33 | 2859.87 |
| **0:00 储电量** | **6000.00** | | **24:00 储电量** | **6000.00** | |

> 上表数值保留 2 位小数，完整精度（4 位小数）见 `结果/result1.xlsx`。

---

## 6. 验证与自检

| 检查项 | 结果 | 结论 |
| --- | --- | --- |
| LP 状态 | `Optimal`（HiGHS Status 7） | 全局最优 |
| 功率平衡最大残差 | 1.82×10⁻¹² kW | ≈ 0，约束满足 |
| SOC 范围 | [1200.0000, 10800.0000] kWh | 落在 [1200, 10800]，且满利用 |
| SOC 首尾 | 6000.0000 / 6000.0000 kWh | 满足能量中性 |
| 效率自洽 | 放电量 = 充电量 × 0.81（16799.94 ≈ 20740.67×0.81） | 往返 81% 一致 |
| 效率损耗 | 购电量 − 净负荷 = 59482.70 − 55541.97 = 3940.73 kWh | = 充电量 × 19%，一致 |
| 弃光量 | 0 kWh | 光伏全部消纳（直供负载 + 存入电池） |

---

## 7. 结果文件说明

### 7.1 文件结构

```
数模国赛C题/
├── 代码/
│   └── problem1.py          # 问题 1 求解脚本（可复现）
├── 结果/
│   └── result1.xlsx         # 问题 1 结果文件（与附件 5 模板同格式）
└── 问题1_求解归档.md         # 本文档
```

### 7.2 result1.xlsx 结构（与附件 5 模板一致）

- **「计划购电量」工作表**：144 行，列为「时间段 / 购电量(kWh)」，保存间隔 10 分钟的购电量。
- **「充放电量」工作表**：6 行 4 小时块 + 0:00/24:00 储电量，列为「时间段 / 充电量 / 放电量 / 时刻 / 储电量」。

### 7.3 时间对齐约定（重要）

附件 1 的时间列是**右端点**标注（`0:10` 代表区间 `[0:00, 0:10]`，共 144 个区间覆盖 `[0:00, 24:00]`）；而 result1 模板的「计划购电量」行标签是**起止点**标注且从 `0:10-0:20` 起步（整体比附件 1 错位 10 分钟）。

因此写入时采用**循环左移 10 分钟**对齐：

```
result1 第 k 行(k=0..142) = 附件1 区间 (k+1) 的购电量
result1 第 143 行(0:00+1-0:10+1) = 附件1 区间 0([0:00,0:10]) 的购电量
```

即 `result1[k] = p[(k+1) % 144]`，其中 `p[i] = g_i · Δt` 为附件 1 顺序下的区间购电量。表 1 的指定窗口（如 `10:00-10:10`）按附件 1 区间索引取（`10:00-10:10` → 索引 60）。

---

## 8. 物理洞察与结论

1. **储能套利逻辑**：凌晨低价（~0.42 元）充电（0:00-4:00 充 4500 kWh），早高峰（6-8 点，~0.9-1.16 元）与晚高峰（18-21 点，~1.2-1.4 元）放电；白天光伏直供负载并反充电池，实现"购电从高价时段搬移到低价时段"。

2. **光伏零弃光**：光伏总能量 55483 kWh 恰好可被负载 + 电池全部消纳，全天无弃光。

3. **电池满利用**：SOC 全程触及 [1200, 10800] 两个边界，电池一天内完成约 2.16 个可用容量（9600 kWh）的充放循环（总充电 20741 kWh）。

4. **效率损耗占比**：因往返效率 81%，储能套利带来约 3941 kWh（≈ 19%×充电量）的额外购电损耗，这是"搬移能量"的代价。

---

## 9. 复现方法

```bash
python 代码/problem1.py
```

脚本会自动：读取 `附件/附件1.xlsx` → 建立并求解 LP → 打印自检与表 1/表 2 → 覆盖写入 `结果/result1.xlsx`。
